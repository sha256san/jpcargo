# Rust コンパイラエラー一覧 (401〜518: E0667 〜 E0806)
> 出典: [Rust Error Codes Index](https://doc.rust-lang.org/error_codes/error-index.html)
> 本ドキュメントは `jpcargo` 開発用エラーデータベースとして、公式ドキュメントおよび rustc 診断情報に基づき整理・日本語化したものです。
---

## 401. E0667 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
`impl Trait` is not allowed in path parameters.

### 日本語解説・原因
このエラーコード（`E0667`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn some_fn(mut x: impl Iterator) -> <impl Iterator>::Item { // error!
    x.next().unwrap()
}
```

---

## 402. E0668 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Malformed inline assembly rejected by LLVM.

### 日本語解説・原因
このエラーコード（`E0668`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(llvm_asm)]

fn main() {
    let rax: u64;
    unsafe {
        llvm_asm!("" :"={rax"(rax));
        println!("Accumulator is: {}", rax);
    }
}
```

---

## 403. E0669 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Cannot convert inline assembly operand to a single LLVM value.

### 日本語解説・原因
このエラーコード（`E0669`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(llvm_asm)]

fn main() {
    unsafe {
        llvm_asm!("" :: "r"("")); // error!
    }
}
```

---

## 404. E0670 

### 概要 (English Summary)
Rust 2015 does not permit the use of `async fn`.

### 日本語解説・原因
`E0670` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Rust 2015 does not permit the use of `async fn`.

### コード例 (Example)
```rust
async fn foo() {}
```

---

## 405. E0671 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Const parameters cannot depend on type parameters.

### 日本語解説・原因
このエラーコード（`E0671`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn const_id<T, const N: T>() -> T { // error
    N
}
```

---

## 406. E0687 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
In-band lifetimes cannot be used in `fn`/`Fn` syntax.

### 日本語解説・原因
このエラーコード（`E0687`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(in_band_lifetimes)]

fn foo(x: fn(&'a u32)) {} // error!

fn bar(x: &Fn(&'a u32)) {} // error!

fn baz(x: fn(&'a u32), y: &'a u32) {} // error!

struct Foo<'a> { x: &'a u32 }

impl Foo<'a> {
    fn bar(&self, x: fn(&'a u32)) {} // error!
}
```

---

## 407. E0688 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
In-band lifetimes were mixed with explicit lifetime binders.

### 日本語解説・原因
このエラーコード（`E0688`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(in_band_lifetimes)]

fn foo<'a>(x: &'a u32, y: &'b u32) {}   // error!

struct Foo<'a> { x: &'a u32 }

impl Foo<'a> {
    fn bar<'b>(x: &'a u32, y: &'b u32, z: &'c u32) {}   // error!
}

impl<'b> Foo<'a> {  // error!
    fn baz() {}
}
```

---

## 408. E0689 

### 概要 (English Summary)
A method was called on an ambiguous numeric type.

### 日本語解説・原因
`E0689` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A method was called on an ambiguous numeric type.

### コード例 (Example)
```rust
2.0.neg(); // error!
```

---

## 409. E0690 

### 概要 (English Summary)
A struct with the representation hint `repr(transparent)` had two or more fields

### 日本語解説・原因
`E0690` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A struct with the representation hint `repr(transparent)` had two or more fields

### コード例 (Example)
```rust
#[repr(transparent)]
struct LengthWithUnit<U> { // error: transparent struct needs at most one
    value: f32,            //        non-zero-sized field, but has 2
    unit: U,
}
```

---

## 410. E0691 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A struct, enum, or union with the `repr(transparent)` representation hint

### 日本語解説・原因
このエラーコード（`E0691`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(repr_align)]

#[repr(align(32))]
struct ForceAlign32;

#[repr(transparent)]
struct Wrapper(f32, ForceAlign32); // error: zero-sized field in transparent
                                   //        struct has alignment of 32, which
                                   //        is larger than 1
```

---

## 411. E0692 

### 概要 (English Summary)
A `repr(transparent)` type was also annotated with other, incompatible

### 日本語解説・原因
`E0692` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A `repr(transparent)` type was also annotated with other, incompatible

### コード例 (Example)
```rust
#[repr(transparent, C)] // error: incompatible representation hints
struct Grams(f32);
```

---

## 412. E0693 

### 概要 (English Summary)
`align` representation hint was incorrectly declared.

### 日本語解説・原因
`E0693` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `align` representation hint was incorrectly declared.

### コード例 (Example)
```rust
#[repr(align=8)] // error!
struct Align8(i8);

#[repr(align="8")] // error!
struct Align8(i8);
```

---

## 413. E0695 

### 概要 (English Summary)
A `break` statement without a label appeared inside a labeled block.

### 日本語解説・原因
`E0695` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A `break` statement without a label appeared inside a labeled block.

### コード例 (Example)
```rust
loop {
    'a: {
        break;
    }
}
```

---

## 414. E0696 

### 概要 (English Summary)
A function is using `continue` keyword incorrectly.

### 日本語解説・原因
`E0696` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A function is using `continue` keyword incorrectly.

### コード例 (Example)
```rust
fn continue_simple() {
    'b: {
        continue; // error!
    }
}
fn continue_labeled() {
    'b: {
        continue 'b; // error!
    }
}
fn continue_crossing() {
    loop {
        'b: {
            continue; // error!
        }
    }
}
```

---

## 415. E0697 

### 概要 (English Summary)
A closure has been used as `static`.

### 日本語解説・原因
`E0697` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A closure has been used as `static`.

### コード例 (Example)
```rust
fn main() {
    static || {}; // used as `static`
}
```

---

## 416. E0698 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
When using coroutines (or async) all type variables must be bound so a

### 日本語解説・原因
このエラーコード（`E0698`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
async fn bar<T>() -> () {}

async fn foo() {
    bar().await; // error: cannot infer type for `T`
}
```

---

## 417. E0699 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A method was called on a raw pointer whose inner type wasn't completely known.

### 日本語解説・原因
このエラーコード（`E0699`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
let foo = &1;
let bar = foo as *const _;
if bar.is_null() {
    // ...
}
```

---

## 418. E0700 

### 概要 (English Summary)
The `impl Trait` return type captures lifetime parameters that do not

### 日本語解説・原因
`E0700` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `impl Trait` return type captures lifetime parameters that do not

### コード例 (Example)
```rust
use std::cell::Cell;

trait Trait<'a> { }

impl<'a, 'b> Trait<'b> for Cell<&'a u32> { }

fn foo<'x, 'y>(x: Cell<&'x u32>) -> impl Trait<'y>
where 'x: 'y
{
    x
}
```

---

## 419. E0701 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This error indicates that a `#[non_exhaustive]` attribute was incorrectly placed

### 日本語解説・原因
このエラーコード（`E0701`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[non_exhaustive]
trait Foo { }
```

---

## 420. E0703 

### 概要 (English Summary)
Invalid ABI (Application Binary Interface) used in the code.

### 日本語解説・原因
`E0703` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Invalid ABI (Application Binary Interface) used in the code.

### コード例 (Example)
```rust
extern "invalid" fn foo() {} // error!
```

---

## 421. E0704 

### 概要 (English Summary)
An incorrect visibility restriction was specified.

### 日本語解説・原因
`E0704` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An incorrect visibility restriction was specified.

### コード例 (Example)
```rust
mod foo {
    pub(foo) struct Bar {
        x: i32
    }
}
```

---

## 422. E0705 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A `#![feature]` attribute was used for a feature that is stable in the

### 日本語解説・原因
このエラーコード（`E0705`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(rust_2018_preview)]
#![feature(test_2018_feature)] // error: the feature
                               // `test_2018_feature` is
                               // included in the Rust 2018 edition
```

---

## 423. E0706 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
`async fn`s are not yet supported in traits in Rust.

### 日本語解説・原因
このエラーコード（`E0706`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
trait T {
    // Neither case is currently supported.
    async fn foo() {}
    async fn bar(&self) {}
}
```

---

## 424. E0708 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
`async` non-`move` closures with parameters are currently not supported.

### 日本語解説・原因
このエラーコード（`E0708`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn main() {
    let add_one = async |num: u8| {
        num + 1
    };
}
```

---

## 425. E0710 

### 概要 (English Summary)
An unknown tool name was found in a scoped lint.

### 日本語解説・原因
`E0710` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unknown tool name was found in a scoped lint.

### コード例 (Example)
```rust
#[allow(clipp::filter_map)] // error!
fn main() {
    // business logic
}
```

---

## 426. E0712 

### 概要 (English Summary)
A borrow of a thread-local variable was made inside a function which outlived

### 日本語解説・原因
`E0712` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A borrow of a thread-local variable was made inside a function which outlived

### コード例 (Example)
```rust
#![feature(thread_local)]

#[thread_local]
static FOO: u8 = 3;

fn main() {
    let a = &FOO; // error: thread-local variable borrowed past end of function

    std::thread::spawn(move || {
        println!("{}", a);
    });
}
```

---

## 427. E0713 

### 概要 (English Summary)
This error occurs when an attempt is made to borrow state past the end of the

### 日本語解説・原因
`E0713` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error occurs when an attempt is made to borrow state past the end of the

### コード例 (Example)
```rust
pub struct S<'a> { data: &'a mut String }

impl<'a> Drop for S<'a> {
    fn drop(&mut self) { self.data.push_str("being dropped"); }
}

fn demo<'a>(s: S<'a>) -> &'a mut String { let p = &mut *s.data; p }
```

---

## 428. E0714 

### 概要 (English Summary)
A `#[marker]` trait contained an associated item.

### 日本語解説・原因
`E0714` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A `#[marker]` trait contained an associated item.

### コード例 (Example)
```rust
#![feature(marker_trait_attr)]
#![feature(associated_type_defaults)]

#[marker]
trait MarkerConst {
    const N: usize; // error!
}

fn main() {}
```

---

## 429. E0715 

### 概要 (English Summary)
An `impl` for a `#[marker]` trait tried to override an associated item.

### 日本語解説・原因
`E0715` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An `impl` for a `#[marker]` trait tried to override an associated item.

### コード例 (Example)
```rust
#![feature(marker_trait_attr)]

#[marker]
trait Marker {
    const N: usize = 0;
    fn do_something() {}
}

struct OverrideConst;
impl Marker for OverrideConst { // error!
    const N: usize = 1;
}
```

---

## 430. E0716 

### 概要 (English Summary)
A temporary value is being dropped while a borrow is still in active use.

### 日本語解説・原因
`E0716` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A temporary value is being dropped while a borrow is still in active use.

### コード例 (Example)
```rust
fn foo() -> i32 { 22 }
fn bar(x: &i32) -> &i32 { x }
let p = bar(&foo());
         // ------ creates a temporary
let q = *p;
```

---

## 431. E0711 

### 概要 (English Summary)
Feature declared with conflicting stability requirements.

### 日本語解説・原因
`E0711` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Feature declared with conflicting stability requirements.

### コード例 (Example)
```rust
// NOTE: this attribute is perma-unstable and should *never* be used outside of
//       stdlib and the compiler.
#![feature(staged_api)]

#![stable(feature = "...", since = "1.0.0")]

#[stable(feature = "foo", since = "1.0.0")]
fn foo_stable_1_0_0() {}

// error: feature `foo` is declared stable since 1.29.0
#[stable(feature = "foo", since = "1.29.0")]
fn foo_stable_1_29_0() {}

// error: feature `foo` is declared unstable
#[unstable(feature = "foo", issue = "none")]
fn foo_unstable() {}
```

---

## 432. E0717 

### 概要 (English Summary)
詳細な説明はありません。

### 日本語解説・原因
`E0717` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: 詳細な説明はありません。

---

## 433. E0718 

### 概要 (English Summary)
A `#[lang = ".."]` attribute was placed on the wrong item type.

### 日本語解説・原因
`E0718` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A `#[lang = ".."]` attribute was placed on the wrong item type.

### コード例 (Example)
```rust
#![feature(lang_items)]

#[lang = "owned_box"]
static X: u32 = 42;
```

---

## 434. E0719 

### 概要 (English Summary)
An associated item was specified more than once in a trait object.

### 日本語解説・原因
`E0719` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An associated item was specified more than once in a trait object.

### コード例 (Example)
```rust
trait FooTrait {}
trait BarTrait {}

// error: associated type `Item` in trait `Iterator` is specified twice
type Foo = dyn Iterator<Item = u32, Item = u32>;
```

---

## 435. E0720 

### 概要 (English Summary)
An `impl Trait` type expands to a recursive type.

### 日本語解説・原因
`E0720` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An `impl Trait` type expands to a recursive type.

### コード例 (Example)
```rust
fn make_recursive_type() -> impl Sized {
    [make_recursive_type(), make_recursive_type()]
}
```

---

## 436. E0722 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This is because it was too specific to the `optimize` attribute.

### 日本語解説・原因
このエラーコード（`E0722`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(optimize_attribute)]

#[optimize(something)] // error: invalid argument
pub fn something() {}
```

---

## 437. E0724 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
`#[ffi_returns_twice]` was used on something other than a foreign function

### 日本語解説・原因
このエラーコード（`E0724`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(ffi_returns_twice)]
#![crate_type = "lib"]

#[ffi_returns_twice] // error!
pub fn foo() {}
```

---

## 438. E0725 

### 概要 (English Summary)
A feature attribute named a feature that was disallowed in the compiler

### 日本語解説・原因
`E0725` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A feature attribute named a feature that was disallowed in the compiler

### コード例 (Example)
```rust
#![feature(never_type)] // error: the feature `never_type` is not in
                        // the list of allowed features
```

---

## 439. E0726 

### 概要 (English Summary)
An argument lifetime was elided in an async function.

### 日本語解説・原因
`E0726` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An argument lifetime was elided in an async function.

### コード例 (Example)
```rust
use futures::executor::block_on;
struct Content<'a> {
    title: &'a str,
    body: &'a str,
}
async fn create(content: Content) { // error: implicit elided
                                    // lifetime not allowed here
    println!("title: {}", content.title);
    println!("body: {}", content.body);
}
let content = Content { title: "Rust", body: "is great!" };
let future = create(content);
block_on(future);
```

---

## 440. E0727 

### 概要 (English Summary)
A `yield` clause was used in an `async` context.

### 日本語解説・原因
`E0727` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A `yield` clause was used in an `async` context.

### コード例 (Example)
```rust
#![feature(coroutines, stmt_expr_attributes)]

fn main() {
    let coroutine = #[coroutine] || {
        async {
            yield;
        }
    };
}
```

---

## 441. E0728 

### 概要 (English Summary)
[`await`] has been used outside [`async`] function or [`async`] block.

### 日本語解説・原因
`E0728` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: [`await`] has been used outside [`async`] function or [`async`] block.

### コード例 (Example)
```rust
#
#
#
#
fn foo() {
    wake_and_yield_once().await // `await` is used outside `async` context
}
```

---

## 442. E0729 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Support for Non-Lexical Lifetimes (NLL) has been included in the Rust compiler

### 日本語解説・原因
このエラーコード（`E0729`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 443. E0730 

### 概要 (English Summary)
An array without a fixed length was pattern-matched.

### 日本語解説・原因
`E0730` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An array without a fixed length was pattern-matched.

### コード例 (Example)
```rust
fn is_123<const N: usize>(x: [u32; N]) -> bool {
    match x {
        [1, 2, ..] => true, // error: cannot pattern-match on an
                            //        array without a fixed length
        _ => false
    }
}
```

---

## 444. E0731 

### 概要 (English Summary)
An enum with the representation hint `repr(transparent)` had zero or more than

### 日本語解説・原因
`E0731` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An enum with the representation hint `repr(transparent)` had zero or more than

### コード例 (Example)
```rust
#[repr(transparent)]
enum Status { // error: transparent enum needs exactly one variant, but has 2
    Errno(u32),
    Ok,
}
```

---

## 445. E0732 

### 概要 (English Summary)
An `enum` with a discriminant must specify a `#[repr(inttype)]`.

### 日本語解説・原因
`E0732` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An `enum` with a discriminant must specify a `#[repr(inttype)]`.

### コード例 (Example)
```rust
enum Enum { // error!
    Unit = 1,
    Tuple() = 2,
    Struct{} = 3,
}
```

---

## 446. E0733 

### 概要 (English Summary)
An [`async`] function used recursion without boxing.

### 日本語解説・原因
`E0733` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An [`async`] function used recursion without boxing.

### コード例 (Example)
```rust
async fn foo(n: usize) {
    if n > 0 {
        foo(n - 1).await;
    }
}
```

---

## 447. E0734 

### 概要 (English Summary)
A stability attribute has been used outside of the standard library.

### 日本語解説・原因
`E0734` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A stability attribute has been used outside of the standard library.

### コード例 (Example)
```rust
#[stable(feature = "a", since = "b")] // invalid
#[unstable(feature = "b", issue = "none")] // invalid
fn foo(){}
```

---

## 448. E0735 

### 概要 (English Summary)
Type parameter defaults cannot use `Self` on structs, enums, or unions.

### 日本語解説・原因
`E0735` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Type parameter defaults cannot use `Self` on structs, enums, or unions.

### コード例 (Example)
```rust
struct Foo<X = Box<Self>> {
    field1: Option<X>,
    field2: Option<X>,
}
// error: type parameters cannot use `Self` in their defaults.
```

---

## 449. E0736 

### 概要 (English Summary)
Functions marked with the `#[naked]` attribute are restricted in what other

### 日本語解説・原因
`E0736` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Functions marked with the `#[naked]` attribute are restricted in what other

### コード例 (Example)
```rust
#[inline]
#[unsafe(naked)]
fn foo() {}
```

---

## 450. E0737 

### 概要 (English Summary)
`#[track_caller]` requires functions to have the `"Rust"` ABI for implicitly

### 日本語解説・原因
`E0737` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `#[track_caller]` requires functions to have the `"Rust"` ABI for implicitly

### コード例 (Example)
```rust
#[track_caller]
extern "C" fn foo() {}
```

---

## 451. E0739 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
`#[track_caller]` must  be applied to a function

### 日本語解説・原因
このエラーコード（`E0739`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[track_caller]
struct Bar {
    a: u8,
}
```

---

## 452. E0740 

### 概要 (English Summary)
A `union` was declared with fields with destructors.

### 日本語解説・原因
`E0740` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A `union` was declared with fields with destructors.

### コード例 (Example)
```rust
union Test {
    a: A, // error!
}

#[derive(Debug)]
struct A(i32);

impl Drop for A {
    fn drop(&mut self) { println!("A"); }
}
```

---

## 453. E0741 

### 概要 (English Summary)
A non-structural-match type was used as the type of a const generic parameter.

### 日本語解説・原因
`E0741` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A non-structural-match type was used as the type of a const generic parameter.

### コード例 (Example)
```rust
#![feature(adt_const_params)]

struct A;

struct B<const X: A>; // error!
```

---

## 454. E0742 

### 概要 (English Summary)
Visibility is restricted to a module which isn't an ancestor of the current

### 日本語解説・原因
`E0742` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Visibility is restricted to a module which isn't an ancestor of the current

### コード例 (Example)
```rust
pub mod sea {}

pub (in crate::sea) struct Shark; // error!

fn main() {}
```

---

## 455. E0743 

### 概要 (English Summary)
The C-variadic type `...` has been nested inside another type.

### 日本語解説・原因
`E0743` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The C-variadic type `...` has been nested inside another type.

### コード例 (Example)
```rust
fn foo2(x: u8, y: &...) {} // error!
```

---

## 456. E0744 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An unsupported expression was used inside a const context.

### 日本語解説・原因
このエラーコード（`E0744`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
const _: i32 = {
    async { 0 }.await
};
```

---

## 457. E0745 

### 概要 (English Summary)
The address of temporary value was taken.

### 日本語解説・原因
`E0745` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The address of temporary value was taken.

### コード例 (Example)
```rust
fn temp_address() {
    let ptr = &raw const 2; // error!
}
```

---

## 458. E0746 

### 概要 (English Summary)
An unboxed trait object was used as a return value.

### 日本語解説・原因
`E0746` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unboxed trait object was used as a return value.

### コード例 (Example)
```rust
trait T {
    fn bar(&self);
}
struct S(usize);
impl T for S {
    fn bar(&self) {}
}

// Having the trait `T` as return type is invalid because
// unboxed trait objects do not have a statically known size:
fn foo() -> dyn T { // error!
    S(42)
}
```

---

## 459. E0747 

### 概要 (English Summary)
Generic arguments were not provided in the same order as the corresponding

### 日本語解説・原因
`E0747` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Generic arguments were not provided in the same order as the corresponding

### コード例 (Example)
```rust
struct S<'a, T>(&'a T);

type X = S<(), 'static>; // error: the type argument is provided before the
                         // lifetime argument
```

---

## 460. E0748 

### 概要 (English Summary)
A raw string isn't correctly terminated because the trailing `#` count doesn't

### 日本語解説・原因
`E0748` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A raw string isn't correctly terminated because the trailing `#` count doesn't

### コード例 (Example)
```rust
let dolphins = r##"Dolphins!"#; // error!
```

---

## 461. E0749 

### 概要 (English Summary)
An item was added on a negative impl.

### 日本語解説・原因
`E0749` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An item was added on a negative impl.

### コード例 (Example)
```rust
trait MyTrait {
    type Foo;
}

impl !MyTrait for u32 {
    type Foo = i32; // error!
}
```

---

## 462. E0750 

### 概要 (English Summary)
A negative impl was made default impl.

### 日本語解説・原因
`E0750` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A negative impl was made default impl.

### コード例 (Example)
```rust
trait MyTrait {
    type Foo;
}

default impl !MyTrait for u32 {} // error!
```

---

## 463. E0751 

### 概要 (English Summary)
There are both a positive and negative trait implementation for the same type.

### 日本語解説・原因
`E0751` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: There are both a positive and negative trait implementation for the same type.

### コード例 (Example)
```rust
trait MyTrait {}
impl MyTrait for i32 { }
impl !MyTrait for i32 { } // error!
```

---

## 464. E0752 

### 概要 (English Summary)
The entry point of the program was marked as `async`.

### 日本語解説・原因
`E0752` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The entry point of the program was marked as `async`.

### コード例 (Example)
```rust
async fn main() -> Result<(), ()> { // error!
    Ok(())
}
```

---

## 465. E0753 

### 概要 (English Summary)
An inner doc comment was used in an invalid context.

### 日本語解説・原因
`E0753` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An inner doc comment was used in an invalid context.

### コード例 (Example)
```rust
fn foo() {}
//! foo
// ^ error!
fn main() {}
```

---

## 466. E0754 

### 概要 (English Summary)
A non-ASCII identifier was used in an invalid context.

### 日本語解説・原因
`E0754` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A non-ASCII identifier was used in an invalid context.

### コード例 (Example)
```rust
mod řųśť; // error!

#[no_mangle]
fn řųśť() {} // error!

fn main() {}
```

---

## 467. E0755 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `ffi_pure` attribute was used on a non-foreign function.

### 日本語解説・原因
このエラーコード（`E0755`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(ffi_pure)]

#[unsafe(ffi_pure)] // error!
pub fn foo() {}
```

---

## 468. E0756 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `ffi_const` attribute was used on something other than a foreign function

### 日本語解説・原因
このエラーコード（`E0756`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(ffi_const)]

#[unsafe(ffi_const)] // error!
pub fn foo() {}
```

---

## 469. E0757 

### 概要 (English Summary)
A function was given both the `ffi_const` and `ffi_pure` attributes.

### 日本語解説・原因
`E0757` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A function was given both the `ffi_const` and `ffi_pure` attributes.

### コード例 (Example)
```rust
#![feature(ffi_const, ffi_pure)]

extern "C" {
    #[unsafe(ffi_const)]
    #[unsafe(ffi_pure)]
    //~^ ERROR `#[ffi_const]` function cannot be `#[ffi_pure]`
    pub fn square(num: i32) -> i32;
}
```

---

## 470. E0758 

### 概要 (English Summary)
A multi-line (doc-)comment is unterminated.

### 日本語解説・原因
`E0758` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A multi-line (doc-)comment is unterminated.

### コード例 (Example)
```rust
/* I am not terminated!
```

---

## 471. E0759 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Return type involving a trait did not require `'static` lifetime.

### 日本語解説・原因
このエラーコード（`E0759`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
use std::fmt::Debug;

fn foo(x: &i32) -> impl Debug { // error!
    x
}

fn bar(x: &i32) -> Box<dyn Debug> { // error!
    Box::new(x)
}
```

---

## 472. E0760 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
`async fn`/`impl trait` return type cannot contain a projection

### 日本語解説・原因
このエラーコード（`E0760`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
struct S<'a>(&'a i32);

impl<'a> S<'a> {
    async fn new(i: &'a i32) -> Self {
        S(&22)
    }
}
```

---

## 473. E0761 

### 概要 (English Summary)
Multiple candidate files were found for an out-of-line module.

### 日本語解説・原因
`E0761` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Multiple candidate files were found for an out-of-line module.

### コード例 (Example)
```rust
// file: ambiguous_module/mod.rs

fn foo() {}

// file: ambiguous_module.rs

fn foo() {}

// file: lib.rs

mod ambiguous_module; // error: file for module `ambiguous_module`
                      // found at both ambiguous_module.rs and
                      // ambiguous_module/mod.rs
```

---

## 474. E0762 

### 概要 (English Summary)
A character literal wasn't ended with a quote.

### 日本語解説・原因
`E0762` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A character literal wasn't ended with a quote.

### コード例 (Example)
```rust
static C: char = '●; // error!
```

---

## 475. E0763 

### 概要 (English Summary)
A byte constant wasn't correctly ended.

### 日本語解説・原因
`E0763` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A byte constant wasn't correctly ended.

### コード例 (Example)
```rust
let c = b'a; // error!
```

---

## 476. E0764 

### 概要 (English Summary)
A mutable reference was used in a constant.

### 日本語解説・原因
`E0764` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A mutable reference was used in a constant.

### コード例 (Example)
```rust
fn main() {
    const OH_NO: &'static mut usize = &mut 1; // error!
}
```

---

## 477. E0765 

### 概要 (English Summary)
A double quote string (`"`) was not terminated.

### 日本語解説・原因
`E0765` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A double quote string (`"`) was not terminated.

### コード例 (Example)
```rust
let s = "; // error!
```

---

## 478. E0766 

### 概要 (English Summary)
A double quote byte string (`b"`) was not terminated.

### 日本語解説・原因
`E0766` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A double quote byte string (`b"`) was not terminated.

### コード例 (Example)
```rust
let s = b"; // error!
```

---

## 479. E0767 

### 概要 (English Summary)
An unreachable label was used.

### 日本語解説・原因
`E0767` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unreachable label was used.

### コード例 (Example)
```rust
'a: loop {
    || {
        loop { break 'a } // error: use of unreachable label `'a`
    };
}
```

---

## 480. E0768 

### 概要 (English Summary)
A number in a non-decimal base has no digits.

### 日本語解説・原因
`E0768` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A number in a non-decimal base has no digits.

### コード例 (Example)
```rust
let s: i32 = 0b; // error!
```

---

## 481. E0769 

### 概要 (English Summary)
A tuple struct or tuple variant was used in a pattern as if it were a struct or

### 日本語解説・原因
`E0769` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A tuple struct or tuple variant was used in a pattern as if it were a struct or

### コード例 (Example)
```rust
enum E {
    A(i32),
}

let e = E::A(42);

match e {
    E::A { number } => { // error!
        println!("{}", number);
    }
}
```

---

## 482. E0770 

### 概要 (English Summary)
The type of a const parameter references other generic parameters.

### 日本語解説・原因
`E0770` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The type of a const parameter references other generic parameters.

### コード例 (Example)
```rust
fn foo<T, const N: T>() {} // error!
```

---

## 483. E0771 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A non-`'static` lifetime was used in a const generic. This is currently not

### 日本語解説・原因
このエラーコード（`E0771`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(adt_const_params, unsized_const_params)]

fn function_with_str<'a, const STRING: &'a str>() {} // error!
```

---

## 484. E0772 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A trait object has some specific lifetime `'1`, but it was used in a way that

### 日本語解説・原因
このエラーコード（`E0772`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
trait BooleanLike {}
trait Person {}

impl BooleanLike for bool {}

impl dyn Person {
    fn is_cool(&self) -> bool {
        // hey you, you're pretty cool
        true
    }
}

fn get_is_cool<'p>(person: &'p dyn Person) -> impl BooleanLike {
    // error: `person` has an anonymous lifetime `'p` but calling
    //        `print_cool_fn` introduces an implicit `'static` lifetime
    //        requirement
    person.is_cool()
}
```

---

## 485. E0773 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This was triggered when multiple macro definitions used the same

### 日本語解説・原因
このエラーコード（`E0773`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 486. E0774 

### 概要 (English Summary)
`derive` was applied on something which is not a struct, a union or an enum.

### 日本語解説・原因
`E0774` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `derive` was applied on something which is not a struct, a union or an enum.

### コード例 (Example)
```rust
trait Foo {
    #[derive(Clone)] // error!
    type Bar;
}
```

---

## 487. E0775 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
`#[cmse_nonsecure_entry]` is only valid for targets with the TrustZone-M

### 日本語解説・原因
このエラーコード（`E0775`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(cmse_nonsecure_entry)]

pub extern "cmse-nonsecure-entry" fn entry_function() {}
```

---

## 488. E0776 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
`#[cmse_nonsecure_entry]` functions require a C ABI

### 日本語解説・原因
このエラーコード（`E0776`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(cmse_nonsecure_entry)]

#[no_mangle]
#[cmse_nonsecure_entry]
pub fn entry_function(input: Vec<u32>) {}
```

---

## 489. E0777 

### 概要 (English Summary)
A literal value was used inside `#[derive]`.

### 日本語解説・原因
`E0777` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A literal value was used inside `#[derive]`.

### コード例 (Example)
```rust
#[derive("Clone")] // error!
struct Foo;
```

---

## 490. E0778 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `instruction_set` attribute was malformed.

### 日本語解説・原因
このエラーコード（`E0778`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(isa_attribute)]

#[instruction_set()] // error: expected one argument
pub fn something() {}
fn main() {}
```

---

## 491. E0779 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An unknown argument was given to the `instruction_set` attribute.

### 日本語解説・原因
このエラーコード（`E0779`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(isa_attribute)]

#[instruction_set(intel::x64)] // error: invalid argument
pub fn something() {}
fn main() {}
```

---

## 492. E0780 

### 概要 (English Summary)
Cannot use `doc(inline)` with anonymous imports

### 日本語解説・原因
`E0780` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Cannot use `doc(inline)` with anonymous imports

### コード例 (Example)
```rust
#[doc(inline)] // error: invalid doc argument
pub use foo::Foo as _;
```

---

## 493. E0781 

### 概要 (English Summary)
The `cmse-nonsecure-call` ABI can only be used with function pointers.

### 日本語解説・原因
`E0781` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `cmse-nonsecure-call` ABI can only be used with function pointers.

### コード例 (Example)
```rust
#![feature(abi_cmse_nonsecure_call)]

pub extern "cmse-nonsecure-call" fn test() {}
```

---

## 494. E0782 

### 概要 (English Summary)
Trait objects must include the `dyn` keyword.

### 日本語解説・原因
`E0782` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Trait objects must include the `dyn` keyword.

### コード例 (Example)
```rust
trait Foo {}
fn test(arg: Box<Foo>) {} // error!
```

---

## 495. E0783 

### 概要 (English Summary)
The range pattern `...` is no longer allowed.

### 日本語解説・原因
`E0783` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The range pattern `...` is no longer allowed.

### コード例 (Example)
```rust
match 2u8 {
    0...9 => println!("Got a number less than 10"), // error!
    _ => println!("Got a number 10 or more"),
}
```

---

## 496. E0784 

### 概要 (English Summary)
A union expression does not have exactly one field.

### 日本語解説・原因
`E0784` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A union expression does not have exactly one field.

### コード例 (Example)
```rust
union Bird {
    pigeon: u8,
    turtledove: u16,
}

let bird = Bird {}; // error
let bird = Bird { pigeon: 0, turtledove: 1 }; // error
```

---

## 497. E0785 

### 概要 (English Summary)
An inherent `impl` was written on a dyn auto trait.

### 日本語解説・原因
`E0785` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An inherent `impl` was written on a dyn auto trait.

### コード例 (Example)
```rust
#![feature(auto_traits)]

auto trait AutoTrait {}

impl dyn AutoTrait {}
```

---

## 498. E0786 

### 概要 (English Summary)
A metadata file was invalid.

### 日本語解説・原因
`E0786` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A metadata file was invalid.

### コード例 (Example)
```rust
use ::foo; // error: found invalid metadata files for crate `foo`
```

---

## 499. E0787 

### 概要 (English Summary)
An unsupported naked function definition.

### 日本語解説・原因
`E0787` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unsupported naked function definition.

### コード例 (Example)
```rust
#[unsafe(naked)]
pub extern "C" fn f() -> u32 {
    42
}
```

---

## 500. E0788 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A `#[coverage(off|on)]` attribute was found in a position where it is not

### 日本語解説・原因
このエラーコード（`E0788`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
unsafe extern "C" {
    #[coverage(off)]
    fn foreign_fn();
}
```

---

## 501. E0789 

### 概要 (English Summary)
The internal `rustc_allowed_through_unstable_modules` attribute must be used

### 日本語解説・原因
`E0789` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The internal `rustc_allowed_through_unstable_modules` attribute must be used

### コード例 (Example)
```rust
// NOTE: both of these attributes are perma-unstable and should *never* be
//       used outside of the compiler and standard library.
#![feature(rustc_attrs)]
#![feature(staged_api)]
#![allow(internal_features)]

#![unstable(feature = "foo_module", reason = "...", issue = "123")]

#[rustc_allowed_through_unstable_modules = "deprecation message"]
// #[stable(feature = "foo", since = "1.0")]
struct Foo;
// ^^^ error: `rustc_allowed_through_unstable_modules` attribute must be
//            paired with a `stable` attribute
```

---

## 502. E0790 

### 概要 (English Summary)
You need to specify a specific implementation of the trait in order to call the

### 日本語解説・原因
`E0790` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: You need to specify a specific implementation of the trait in order to call the

### コード例 (Example)
```rust
trait Coroutine {
    fn create() -> u32;
}

struct Impl;

impl Coroutine for Impl {
    fn create() -> u32 { 1 }
}

struct AnotherImpl;

impl Coroutine for AnotherImpl {
    fn create() -> u32 { 2 }
}

let cont: u32 = Coroutine::create();
// error, impossible to choose one of Coroutine trait implementation
// Should it be Impl or AnotherImpl, maybe something else?
```

---

## 503. E0791 

### 概要 (English Summary)
Static variables with the `#[linkage]` attribute within external blocks

### 日本語解説・原因
`E0791` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Static variables with the `#[linkage]` attribute within external blocks

### コード例 (Example)
```rust
#![feature(linkage)]

extern "C" {
    #[linkage = "extern_weak"]
    static foo: i8;
}
```

---

## 504. E0792 

### 概要 (English Summary)
A type alias impl trait can only have its hidden type assigned

### 日本語解説・原因
`E0792` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type alias impl trait can only have its hidden type assigned

### コード例 (Example)
```rust
#![feature(type_alias_impl_trait)]

type Foo<T> = impl std::fmt::Debug;

#[define_opaque(Foo)]
fn foo() -> Foo<u32> {
    5u32
}
```

---

## 505. E0793 

### 概要 (English Summary)
An unaligned reference to a field of a [packed] `struct` or `union` was created.

### 日本語解説・原因
`E0793` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unaligned reference to a field of a [packed] `struct` or `union` was created.

### コード例 (Example)
```rust
#[repr(packed)]
pub struct Foo {
    field1: u64,
    field2: u8,
}

unsafe {
    let foo = Foo { field1: 0, field2: 0 };
    // Accessing the field directly is fine.
    let val = foo.field1;
    // A reference to a packed field causes a error.
    let val = &foo.field1; // ERROR
    // An implicit `&` is added in format strings, causing the same error.
    println!("{}", foo.field1); // ERROR
}
```

---

## 506. E0794 

### 概要 (English Summary)
A lifetime parameter of a function definition is called *late-bound* if it both:

### 日本語解説・原因
`E0794` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A lifetime parameter of a function definition is called *late-bound* if it both:

### コード例 (Example)
```rust
fn foo<'a>(x: &'a str) -> &'a str { x }
let _ = foo::<'static>;
```

---

## 507. E0795 

### 概要 (English Summary)
Invalid argument for the `offset_of!` macro.

### 日本語解説・原因
`E0795` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Invalid argument for the `offset_of!` macro.

### コード例 (Example)
```rust
#![feature(offset_of_enum)]

let x = std::mem::offset_of!(Option<u8>, Some);
```

---

## 508. E0796 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
You have created a reference to a mutable static.

### 日本語解説・原因
このエラーコード（`E0796`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
static mut X: i32 = 23;
fn work() {
  let _val = unsafe { X };
}
let x_ref = unsafe { &mut X };
work();
// The next line has Undefined Behavior!
// `x_ref` is a mutable reference and allows no aliases,
// but `work` has been reading the reference between
// the moment `x_ref` was created and when it was used.
// This violates the uniqueness of `x_ref`.
*x_ref = 42;
```

---

## 509. E0797 

### 概要 (English Summary)
Struct update syntax was used without a base expression.

### 日本語解説・原因
`E0797` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Struct update syntax was used without a base expression.

### コード例 (Example)
```rust
struct Foo {
    fizz: u8,
    buzz: u8
}

let f1 = Foo { fizz: 10, buzz: 1};
let f2 = Foo { fizz: 10, .. }; // error
```

---

## 510. E0798 

### 概要 (English Summary)
Functions marked as `cmse-nonsecure-call` place restrictions on their

### 日本語解説・原因
`E0798` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Functions marked as `cmse-nonsecure-call` place restrictions on their

### コード例 (Example)
```rust
#![feature(abi_cmse_nonsecure_call)]

#[no_mangle]
pub fn test(
    f: extern "cmse-nonsecure-call" fn(u32, u32, u32, u32, u32) -> u32,
) -> u32 {
    f(1, 2, 3, 4, 5)
}
```

---

## 511. E0799 

### 概要 (English Summary)
Something other than a type or const parameter has been used when one was

### 日本語解説・原因
`E0799` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Something other than a type or const parameter has been used when one was

### コード例 (Example)
```rust
fn bad1() -> impl Sized + use<main> {}

fn bad2(x: ()) -> impl Sized + use<x> {}

fn main() {}
```

---

## 512. E0800 

### 概要 (English Summary)
A type or const parameter of the given name is not in scope.

### 日本語解説・原因
`E0800` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type or const parameter of the given name is not in scope.

### コード例 (Example)
```rust
fn missing() -> impl Sized + use<T> {}
```

---

## 513. E0801 

### 概要 (English Summary)
The `self` parameter in a method has an invalid generic "receiver type".

### 日本語解説・原因
`E0801` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `self` parameter in a method has an invalid generic "receiver type".

### コード例 (Example)
```rust
struct Foo;

impl Foo {
    fn foo<R: std::ops::Deref<Target=Self>>(self: R) {}
}
```

---

## 514. E0802 

### 概要 (English Summary)
The target of `derive(CoercePointee)` macro has inadmissible specification for

### 日本語解説・原因
`E0802` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The target of `derive(CoercePointee)` macro has inadmissible specification for

### コード例 (Example)
```rust
#![feature(coerce_pointee)]
use std::marker::CoercePointee;
#[derive(CoercePointee)]
enum NotStruct<'a, T: ?Sized> {
    Variant(&'a T),
}
```

---

## 515. E0803 

### 概要 (English Summary)
A trait implementation returns a reference without an

### 日本語解説・原因
`E0803` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A trait implementation returns a reference without an

### コード例 (Example)
```rust
trait DataAccess<T> {
    fn get_ref(&self) -> T;
}

struct Container<'a> {
    value: &'a f64,
}

// Attempting to implement reference return
impl<'a> DataAccess<&f64> for Container<'a> {
    fn get_ref(&self) -> &f64 { // Error: Lifetime mismatch
        self.value
    }
}
```

---

## 516. E0804 

### 概要 (English Summary)
An auto trait cannot be added to the bounds of a `dyn Trait` type via

### 日本語解説・原因
`E0804` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An auto trait cannot be added to the bounds of a `dyn Trait` type via

### コード例 (Example)
```rust
let ptr: *const dyn core::any::Any = &();
_ = ptr as *const (dyn core::any::Any + Send);
```

---

## 517. E0805 

### 概要 (English Summary)
An attribute was given an invalid number of arguments

### 日本語解説・原因
`E0805` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An attribute was given an invalid number of arguments

### コード例 (Example)
```rust
#[inline()] // error! should either have a single argument, or no parentheses
fn foo() {}

#[inline(always, never)] // error! should have only one argument, not two
fn bar() {}
```

---

## 518. E0806 

### 概要 (English Summary)
An externally implementable item is not compatible with its declaration.

### 日本語解説・原因
`E0806` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An externally implementable item is not compatible with its declaration.

### コード例 (Example)
```rust
#![feature(extern_item_impls)]

#[eii(foo)]
fn x();

#[foo]
fn y(a: u64) -> u64 {
//~^ ERROR E0806
    a
}


fn main() {}
```

---

