# Rust コンパイラエラー一覧 (201〜400: E0408 〜 E0666)
> 出典: [Rust Error Codes Index](https://doc.rust-lang.org/error_codes/error-index.html)
> 本ドキュメントは `jpcargo` 開発用エラーデータベースとして、公式ドキュメントおよび rustc 診断情報に基づき整理・日本語化したものです。
---

## 201. E0408 

### 概要 (English Summary)
An "or" pattern was used where the variable bindings are not consistently bound

### 日本語解説・原因
`E0408` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An "or" pattern was used where the variable bindings are not consistently bound

### コード例 (Example)
```rust
match x {
    Some(y) | None => { /* use y */ } // error: variable `y` from pattern #1 is
                                      //        not bound in pattern #2
    _ => ()
}
```

---

## 202. E0409 

### 概要 (English Summary)
An "or" pattern was used where the variable bindings are not consistently bound

### 日本語解説・原因
`E0409` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An "or" pattern was used where the variable bindings are not consistently bound

### コード例 (Example)
```rust
let x = (0, 2);
match x {
    (0, ref y) | (y, 0) => { /* use y */} // error: variable `y` is bound with
                                          //        different mode in pattern #2
                                          //        than in pattern #1
    _ => ()
}
```

---

## 203. E0411 

### 概要 (English Summary)
The `Self` keyword was used outside an impl, trait, or type definition.

### 日本語解説・原因
`E0411` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `Self` keyword was used outside an impl, trait, or type definition.

### コード例 (Example)
```rust
<Self>::foo; // error: use of `Self` outside of an impl, trait, or type
             // definition
```

---

## 204. E0412 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Erroneous code examples:

### 日本語解説・原因
このエラーコード（`E0412`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
impl Something {} // error: type name `Something` is not in scope

// or:

trait Foo {
    fn bar(N); // error: type name `N` is not in scope
}

// or:

fn foo(x: T) {} // type name `T` is not in scope
```

---

## 205. E0415 

### 概要 (English Summary)
More than one function parameter have the same name.

### 日本語解説・原因
`E0415` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: More than one function parameter have the same name.

### コード例 (Example)
```rust
fn foo(f: i32, f: i32) {} // error: identifier `f` is bound more than
                          //        once in this parameter list
```

---

## 206. E0416 

### 概要 (English Summary)
An identifier is bound more than once in a pattern.

### 日本語解説・原因
`E0416` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An identifier is bound more than once in a pattern.

### コード例 (Example)
```rust
match (1, 2) {
    (x, x) => {} // error: identifier `x` is bound more than once in the
                 //        same pattern
}
```

---

## 207. E0422 

### 概要 (English Summary)
An identifier that is neither defined nor a struct was used.

### 日本語解説・原因
`E0422` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An identifier that is neither defined nor a struct was used.

### コード例 (Example)
```rust
fn main () {
    let x = Foo { x: 1, y: 2 };
}
```

---

## 208. E0423 

### 概要 (English Summary)
An identifier was used like a function name or a value was expected and the

### 日本語解説・原因
`E0423` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An identifier was used like a function name or a value was expected and the

### コード例 (Example)
```rust
struct Foo { a: bool };

let f = Foo();
// error: expected function, tuple struct or tuple variant, found `Foo`
// `Foo` is a struct name, but this expression uses it like a function name
```

---

## 209. E0424 

### 概要 (English Summary)
The `self` keyword was used inside of an associated function without a "`self`

### 日本語解説・原因
`E0424` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `self` keyword was used inside of an associated function without a "`self`

### コード例 (Example)
```rust
struct Foo;

impl Foo {
    // `bar` is a method, because it has a receiver parameter.
    fn bar(&self) {}

    // `foo` is not a method, because it has no receiver parameter.
    fn foo() {
        self.bar(); // error: `self` value is a keyword only available in
                    //        methods with a `self` parameter
    }
}
```

---

## 210. E0425 

### 概要 (English Summary)
An unresolved name was used.

### 日本語解説・原因
`E0425` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unresolved name was used.

### コード例 (Example)
```rust
something_that_doesnt_exist::foo;
// error: unresolved name `something_that_doesnt_exist::foo`

// or:

trait Foo {
    fn bar() {
        Self; // error: unresolved name `Self`
    }
}

// or:

let x = unknown_variable;  // error: unresolved name `unknown_variable`
```

---

## 211. E0426 

### 概要 (English Summary)
An undeclared label was used.

### 日本語解説・原因
`E0426` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An undeclared label was used.

### コード例 (Example)
```rust
loop {
    break 'a; // error: use of undeclared label `'a`
}
```

---

## 212. E0428 

### 概要 (English Summary)
A type or module has been defined more than once.

### 日本語解説・原因
`E0428` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type or module has been defined more than once.

### コード例 (Example)
```rust
struct Bar;
struct Bar; // error: duplicate definition of value `Bar`
```

---

## 213. E0429 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `self` keyword cannot appear alone as the last segment in a `use`

### 日本語解説・原因
このエラーコード（`E0429`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
use std::fmt::self; // error: `self` imports are only allowed within a { } list
```

---

## 214. E0430 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `self` import appears more than once in the list.

### 日本語解説・原因
このエラーコード（`E0430`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
use something::{self, self}; // error: `self` import can only appear once in
                             //        the list
```

---

## 215. E0431 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An invalid `self` import was made.

### 日本語解説・原因
このエラーコード（`E0431`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
use {self}; // error: `self` import can only appear in an import list with a
            //        non-empty prefix
```

---

## 216. E0432 

### 概要 (English Summary)
An import was unresolved.

### 日本語解説・原因
`E0432` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An import was unresolved.

### コード例 (Example)
```rust
use something::Foo; // error: unresolved import `something::Foo`.
```

---

## 217. E0433 

### 概要 (English Summary)
An undeclared crate, module, or type was used.

### 日本語解説・原因
`E0433` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An undeclared crate, module, or type was used.

### コード例 (Example)
```rust
let map = HashMap::new();
// error: failed to resolve: use of undeclared type `HashMap`
```

---

## 218. E0434 

### 概要 (English Summary)
A variable used inside an inner function comes from a dynamic environment.

### 日本語解説・原因
`E0434` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A variable used inside an inner function comes from a dynamic environment.

### コード例 (Example)
```rust
fn foo() {
    let y = 5;
    fn bar() -> u32 {
        y // error: can't capture dynamic environment in a fn item; use the
          //        || { ... } closure form instead.
    }
}
```

---

## 219. E0435 

### 概要 (English Summary)
A non-constant value was used in a constant expression.

### 日本語解説・原因
`E0435` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A non-constant value was used in a constant expression.

### コード例 (Example)
```rust
let foo = 42;
let a: [u8; foo]; // error: attempt to use a non-constant value in a constant
```

---

## 220. E0436 

### 概要 (English Summary)
The functional record update syntax was used on something other than a struct.

### 日本語解説・原因
`E0436` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The functional record update syntax was used on something other than a struct.

### コード例 (Example)
```rust
enum PublicationFrequency {
    Weekly,
    SemiMonthly { days: (u8, u8), annual_special: bool },
}

fn one_up_competitor(competitor_frequency: PublicationFrequency)
                     -> PublicationFrequency {
    match competitor_frequency {
        PublicationFrequency::Weekly => PublicationFrequency::SemiMonthly {
            days: (1, 15), annual_special: false
        },
        c @ PublicationFrequency::SemiMonthly{ .. } =>
            PublicationFrequency::SemiMonthly {
                annual_special: true, ..c // error: functional record update
                                          //        syntax requires a struct
        }
    }
}
```

---

## 221. E0437 

### 概要 (English Summary)
An associated type whose name does not match any of the associated types

### 日本語解説・原因
`E0437` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An associated type whose name does not match any of the associated types

### コード例 (Example)
```rust
trait Foo {}

impl Foo for i32 {
    type Bar = bool;
}
```

---

## 222. E0438 

### 概要 (English Summary)
An associated constant whose name does not match any of the associated constants

### 日本語解説・原因
`E0438` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An associated constant whose name does not match any of the associated constants

### コード例 (Example)
```rust
trait Foo {}

impl Foo for i32 {
    const BAR: bool = true;
}
```

---

## 223. E0439 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The length of the platform-intrinsic function `simd_shuffle` wasn't specified.

### 日本語解説・原因
このエラーコード（`E0439`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(platform_intrinsics)]

extern "platform-intrinsic" {
    fn simd_shuffle<A,B>(a: A, b: A, c: [u32; 8]) -> B;
    // error: invalid `simd_shuffle`, needs length: `simd_shuffle`
}
```

---

## 224. E0445 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A private trait was used on a public type parameter bound.

### 日本語解説・原因
このエラーコード（`E0445`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
trait Foo {
    fn dummy(&self) { }
}

pub trait Bar : Foo {} // error: private trait in public interface
pub struct Bar2<T: Foo>(pub T); // same error
pub fn foo<T: Foo> (t: T) {} // same error

fn main() {}
```

---

## 225. E0446 

### 概要 (English Summary)
A private type or trait was used in a public associated type signature.

### 日本語解説・原因
`E0446` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A private type or trait was used in a public associated type signature.

### コード例 (Example)
```rust
struct Bar;

pub trait PubTr {
    type Alias;
}

impl PubTr for u8 {
    type Alias = Bar; // error private type in public interface
}

fn main() {}
```

---

## 226. E0447 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `pub` keyword was used inside a function.

### 日本語解説・原因
このエラーコード（`E0447`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn foo() {
    pub struct Bar; // error: visibility has no effect inside functions
}
```

---

## 227. E0448 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `pub` keyword was used inside a public enum.

### 日本語解説・原因
このエラーコード（`E0448`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
pub enum Foo {
    pub Bar, // error: unnecessary `pub` visibility
}
```

---

## 228. E0449 

### 概要 (English Summary)
A visibility qualifier was used where one is not permitted. Visibility

### 日本語解説・原因
`E0449` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A visibility qualifier was used where one is not permitted. Visibility

### コード例 (Example)
```rust
struct Bar;

trait Foo {
    fn foo();
}

enum Baz {
    pub Qux, // error: visibility qualifiers are not permitted here
}

pub impl Bar {} // error: visibility qualifiers are not permitted here

pub impl Foo for Bar { // error: visibility qualifiers are not permitted here
    pub fn foo() {} // error: visibility qualifiers are not permitted here
}
```

---

## 229. E0451 

### 概要 (English Summary)
A struct constructor with private fields was invoked.

### 日本語解説・原因
`E0451` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A struct constructor with private fields was invoked.

### コード例 (Example)
```rust
mod bar {
    pub struct Foo {
        pub a: isize,
        b: isize,
    }
}

let f = bar::Foo{ a: 0, b: 0 }; // error: field `b` of struct `bar::Foo`
                                //        is private
```

---

## 230. E0452 

### 概要 (English Summary)
An invalid lint attribute has been given.

### 日本語解説・原因
`E0452` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An invalid lint attribute has been given.

### コード例 (Example)
```rust
#![allow(foo = "")] // error: malformed lint attribute
```

---

## 231. E0453 

### 概要 (English Summary)
A lint check attribute was overruled by a `forbid` directive set as an

### 日本語解説・原因
`E0453` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A lint check attribute was overruled by a `forbid` directive set as an

### コード例 (Example)
```rust
#![forbid(non_snake_case)]

#[allow(non_snake_case)]
fn main() {
    // error: allow(non_snake_case) incompatible with previous forbid
    let MyNumber = 2;
}
```

---

## 232. E0454 

### 概要 (English Summary)
A link name was given with an empty name.

### 日本語解説・原因
`E0454` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A link name was given with an empty name.

### コード例 (Example)
```rust
#[link(name = "")] extern "C" {}
// error: `#[link(name = "")]` given with empty name
```

---

## 233. E0455 

### 概要 (English Summary)
Some linking kinds are target-specific and not supported on all platforms.

### 日本語解説・原因
`E0455` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Some linking kinds are target-specific and not supported on all platforms.

### コード例 (Example)
```rust
#[link(name = "FooCoreServices", kind = "framework")] extern "C" {}
// OS used to compile is Linux for example
```

---

## 234. E0457 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Plugin `..` only found in rlib format, but must be available in dylib format.

### 日本語解説・原因
このエラーコード（`E0457`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![crate_type = "rlib"]
#![feature(rustc_private)]

extern crate rustc_middle;
extern crate rustc_driver;

use rustc_driver::plugin::Registry;

#[no_mangle]
fn __rustc_plugin_registrar(_: &mut Registry) {}
```

---

## 235. E0458 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An unknown "kind" was specified for a link attribute.

### 日本語解説・原因
このエラーコード（`E0458`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[link(kind = "wonderful_unicorn")] extern "C" {}
// error: unknown kind: `wonderful_unicorn`
```

---

## 236. E0459 

### 概要 (English Summary)
A link was used without a name parameter.

### 日本語解説・原因
`E0459` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A link was used without a name parameter.

### コード例 (Example)
```rust
#[link(kind = "dylib")] extern "C" {}
// error: `#[link(...)]` specified without `name = "foo"`
```

---

## 237. E0460 

### 概要 (English Summary)
Found possibly newer version of crate `..` which `..` depends on.

### 日本語解説・原因
`E0460` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Found possibly newer version of crate `..` which `..` depends on.

### コード例 (Example)
```rust
#![crate_name = "a"]

pub fn foo<T>() {}
```

---

## 238. E0461 

### 概要 (English Summary)
Couldn't find crate `..` with expected target triple `..`.

### 日本語解説・原因
`E0461` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Couldn't find crate `..` with expected target triple `..`.

### コード例 (Example)
```rust
#![crate_type = "lib"]

fn foo() {}
```

---

## 239. E0462 

### 概要 (English Summary)
Found `staticlib` `..` instead of `rlib` or `dylib`.

### 日本語解説・原因
`E0462` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Found `staticlib` `..` instead of `rlib` or `dylib`.

### コード例 (Example)
```rust
#![crate_type = "staticlib"]

fn foo() {}
```

---

## 240. E0463 

### 概要 (English Summary)
A crate was declared but cannot be found.

### 日本語解説・原因
`E0463` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A crate was declared but cannot be found.

### コード例 (Example)
```rust
extern crate foo; // error: can't find crate
```

---

## 241. E0464 

### 概要 (English Summary)
The compiler found multiple library files with the requested crate name.

### 日本語解説・原因
`E0464` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The compiler found multiple library files with the requested crate name.

### コード例 (Example)
```rust
// aux-build:crateresolve-1.rs
// aux-build:crateresolve-2.rs
// aux-build:crateresolve-3.rs

extern crate crateresolve;
//~^ ERROR multiple candidates for `rlib` dependency `crateresolve` found

fn main() {}
```

---

## 242. E0466 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Macro import declaration was malformed.

### 日本語解説・原因
このエラーコード（`E0466`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[macro_use(a_macro(another_macro))] // error: invalid import declaration
extern crate core as some_crate;

#[macro_use(i_want = "some_macros")] // error: invalid import declaration
extern crate core as another_crate;
```

---

## 243. E0468 

### 概要 (English Summary)
A non-root module tried to import macros from another crate.

### 日本語解説・原因
`E0468` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A non-root module tried to import macros from another crate.

### コード例 (Example)
```rust
mod foo {
    #[macro_use(debug_assert)]  // error: must be at crate root to import
    extern crate core;          //        macros from another crate
    fn run_macro() { debug_assert!(true); }
}
```

---

## 244. E0469 

### 概要 (English Summary)
A macro listed for import was not found.

### 日本語解説・原因
`E0469` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A macro listed for import was not found.

### コード例 (Example)
```rust
#[macro_use(drink, be_merry)] // error: imported macro not found
extern crate alloc;

fn main() {
    // ...
}
```

---

## 245. E0472 

### 概要 (English Summary)
Inline assembly (`asm!`) is not supported on this target.

### 日本語解説・原因
`E0472` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Inline assembly (`asm!`) is not supported on this target.

### コード例 (Example)
```rust
// compile-flags: --target sparc64-unknown-linux-gnu
#![no_std]

use core::arch::asm;

fn main() {
    unsafe {
        asm!(""); // error: inline assembly is not supported on this target
    }
}
```

---

## 246. E0476 

### 概要 (English Summary)
The coerced type does not outlive the value being coerced to.

### 日本語解説・原因
`E0476` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The coerced type does not outlive the value being coerced to.

### コード例 (Example)
```rust
#![feature(coerce_unsized)]
#![feature(unsize)]

use std::marker::Unsize;
use std::ops::CoerceUnsized;

// error: lifetime of the source pointer does not outlive lifetime bound of the
//        object type
impl<'a, 'b, T, S> CoerceUnsized<&'a T> for &'b S where S: Unsize<T> {}
```

---

## 247. E0477 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The type does not fulfill the required lifetime.

### 日本語解説・原因
このエラーコード（`E0477`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
use std::sync::Mutex;

struct MyString<'a> {
    data: &'a str,
}

fn i_want_static_closure<F>(a: F)
    where F: Fn() + 'static {}

fn print_string<'a>(s: Mutex<MyString<'a>>) {

    i_want_static_closure(move || {     // error: this closure has lifetime 'a
                                        //        rather than 'static
        println!("{}", s.lock().unwrap().data);
    });
}
```

---

## 248. E0478 

### 概要 (English Summary)
A lifetime bound was not satisfied.

### 日本語解説・原因
`E0478` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A lifetime bound was not satisfied.

### コード例 (Example)
```rust
// Check that the explicit lifetime bound (`'SnowWhite`, in this example) must
// outlive all the superbounds from the trait (`'kiss`, in this example).

trait Wedding<'t>: 't { }

struct Prince<'kiss, 'SnowWhite> {
    child: Box<Wedding<'kiss> + 'SnowWhite>,
    // error: lifetime bound not satisfied
}
```

---

## 249. E0482 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A lifetime of a returned value does not outlive the function call.

### 日本語解説・原因
このエラーコード（`E0482`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn prefix<'a>(
    words: impl Iterator<Item = &'a str>
) -> impl Iterator<Item = String> { // error!
    words.map(|v| format!("foo-{}", v))
}
```

---

## 250. E0491 

### 概要 (English Summary)
A reference has a longer lifetime than the data it references.

### 日本語解説・原因
`E0491` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A reference has a longer lifetime than the data it references.

### コード例 (Example)
```rust
struct Foo<'a> {
    x: fn(&'a i32),
}

trait Trait<'a, 'b> {
    type Out;
}

impl<'a, 'b> Trait<'a, 'b> for usize {
    type Out = &'a Foo<'b>; // error!
}
```

---

## 251. E0492 

### 概要 (English Summary)
A borrow of a constant containing interior mutability was attempted.

### 日本語解説・原因
`E0492` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A borrow of a constant containing interior mutability was attempted.

### コード例 (Example)
```rust
use std::sync::atomic::AtomicUsize;

const A: AtomicUsize = AtomicUsize::new(0);
const B: &'static AtomicUsize = &A;
// error: cannot borrow a constant which may contain interior mutability,
//        create a static instead
```

---

## 252. E0493 

### 概要 (English Summary)
A value with a custom `Drop` implementation may be dropped during const-eval.

### 日本語解説・原因
`E0493` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A value with a custom `Drop` implementation may be dropped during const-eval.

### コード例 (Example)
```rust
enum DropType {
    A,
}

impl Drop for DropType {
    fn drop(&mut self) {}
}

struct Foo {
    field1: DropType,
}

static FOO: Foo = Foo { field1: (DropType::A, DropType::A).1 }; // error!
```

---

## 253. E0495 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A lifetime cannot be determined in the given situation.

### 日本語解説・原因
このエラーコード（`E0495`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn transmute_lifetime<'a, 'b, T>(t: &'a (T,)) -> &'b T {
    match (&t,) { // error!
        ((u,),) => u,
    }
}

let y = Box::new((42,));
let x = transmute_lifetime(&y);
```

---

## 254. E0496 

### 概要 (English Summary)
A lifetime name is shadowing another lifetime name.

### 日本語解説・原因
`E0496` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A lifetime name is shadowing another lifetime name.

### コード例 (Example)
```rust
struct Foo<'a> {
    a: &'a i32,
}

impl<'a> Foo<'a> {
    fn f<'a>(x: &'a i32) { // error: lifetime name `'a` shadows a lifetime
                           //        name that is already in scope
    }
}
```

---

## 255. E0497 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A stability attribute was used outside of the standard library.

### 日本語解説・原因
このエラーコード（`E0497`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[stable] // error: stability attributes may not be used outside of the
          //        standard library
fn foo() {}
```

---

## 256. E0498 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `plugin` attribute was malformed.

### 日本語解説・原因
このエラーコード（`E0498`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(plugin)]
#![plugin(foo(args))] // error: invalid argument
#![plugin(bar="test")] // error: invalid argument
```

---

## 257. E0499 

### 概要 (English Summary)
A variable was borrowed as mutable more than once.

### 日本語解説・原因
`E0499` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A variable was borrowed as mutable more than once.

### コード例 (Example)
```rust
let mut i = 0;
let mut x = &mut i;
let mut a = &mut i;
x;
// error: cannot borrow `i` as mutable more than once at a time
```

---

## 258. E0500 

### 概要 (English Summary)
A borrowed variable was used by a closure.

### 日本語解説・原因
`E0500` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A borrowed variable was used by a closure.

### コード例 (Example)
```rust
fn you_know_nothing(jon_snow: &mut i32) {
    let nights_watch = &jon_snow;
    let starks = || {
        *jon_snow = 3; // error: closure requires unique access to `jon_snow`
                       //        but it is already borrowed
    };
    println!("{}", nights_watch);
}
```

---

## 259. E0501 

### 概要 (English Summary)
A mutable variable is used but it is already captured by a closure.

### 日本語解説・原因
`E0501` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A mutable variable is used but it is already captured by a closure.

### コード例 (Example)
```rust
fn inside_closure(x: &mut i32) {
    // Actions which require unique access
}

fn outside_closure(x: &mut i32) {
    // Actions which require unique access
}

fn foo(a: &mut i32) {
    let mut bar = || {
        inside_closure(a)
    };
    outside_closure(a); // error: cannot borrow `*a` as mutable because previous
                        //        closure requires unique access.
    bar();
}
```

---

## 260. E0502 

### 概要 (English Summary)
A variable already borrowed with a certain mutability (either mutable or

### 日本語解説・原因
`E0502` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A variable already borrowed with a certain mutability (either mutable or

### コード例 (Example)
```rust
fn bar(x: &mut i32) {}
fn foo(a: &mut i32) {
    let y = &a; // a is borrowed as immutable.
    bar(a); // error: cannot borrow `*a` as mutable because `a` is also borrowed
            //        as immutable
    println!("{}", y);
}
```

---

## 261. E0503 

### 概要 (English Summary)
A value was used after it was mutably borrowed.

### 日本語解説・原因
`E0503` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A value was used after it was mutably borrowed.

### コード例 (Example)
```rust
fn main() {
    let mut value = 3;
    // Create a mutable borrow of `value`.
    let borrow = &mut value;
    let _sum = value + 1; // error: cannot use `value` because
                          //        it was mutably borrowed
    println!("{}", borrow);
}
```

---

## 262. E0504 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This error occurs when an attempt is made to move a borrowed variable into a

### 日本語解説・原因
このエラーコード（`E0504`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
struct FancyNum {
    num: u8,
}

fn main() {
    let fancy_num = FancyNum { num: 5 };
    let fancy_ref = &fancy_num;

    let x = move || {
        println!("child function: {}", fancy_num.num);
        // error: cannot move `fancy_num` into closure because it is borrowed
    };

    x();
    println!("main function: {}", fancy_ref.num);
}
```

---

## 263. E0505 

### 概要 (English Summary)
A value was moved out while it was still borrowed.

### 日本語解説・原因
`E0505` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A value was moved out while it was still borrowed.

### コード例 (Example)
```rust
struct Value {}

fn borrow(val: &Value) {}

fn eat(val: Value) {}

fn main() {
    let x = Value{};
    let _ref_to_val: &Value = &x;
    eat(x);
    borrow(_ref_to_val);
}
```

---

## 264. E0506 

### 概要 (English Summary)
An attempt was made to assign to a borrowed value.

### 日本語解説・原因
`E0506` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An attempt was made to assign to a borrowed value.

### コード例 (Example)
```rust
struct FancyNum {
    num: u8,
}

let mut fancy_num = FancyNum { num: 5 };
let fancy_ref = &fancy_num;
fancy_num = FancyNum { num: 6 };
// error: cannot assign to `fancy_num` because it is borrowed

println!("Num: {}, Ref: {}", fancy_num.num, fancy_ref.num);
```

---

## 265. E0507 

### 概要 (English Summary)
A borrowed value was moved out.

### 日本語解説・原因
`E0507` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A borrowed value was moved out.

### コード例 (Example)
```rust
use std::cell::RefCell;

struct TheDarkKnight;

impl TheDarkKnight {
    fn nothing_is_true(self) {}
}

fn main() {
    let x = RefCell::new(TheDarkKnight);

    x.borrow().nothing_is_true(); // error: cannot move out of borrowed content
}
```

---

## 266. E0508 

### 概要 (English Summary)
A value was moved out of a non-copy fixed-size array.

### 日本語解説・原因
`E0508` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A value was moved out of a non-copy fixed-size array.

### コード例 (Example)
```rust
struct NonCopy;

fn main() {
    let array = [NonCopy; 1];
    let _value = array[0]; // error: cannot move out of type `[NonCopy; 1]`,
                           //        a non-copy fixed-size array
}
```

---

## 267. E0509 

### 概要 (English Summary)
This error occurs when an attempt is made to move out of a value whose type

### 日本語解説・原因
`E0509` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error occurs when an attempt is made to move out of a value whose type

### コード例 (Example)
```rust
struct FancyNum {
    num: usize
}

struct DropStruct {
    fancy: FancyNum
}

impl Drop for DropStruct {
    fn drop(&mut self) {
        // Destruct DropStruct, possibly using FancyNum
    }
}

fn main() {
    let drop_struct = DropStruct{fancy: FancyNum{num: 5}};
    let fancy_field = drop_struct.fancy; // Error E0509
    println!("Fancy: {}", fancy_field.num);
    // implicit call to `drop_struct.drop()` as drop_struct goes out of scope
}
```

---

## 268. E0510 

### 概要 (English Summary)
The matched value was assigned in a match guard.

### 日本語解説・原因
`E0510` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The matched value was assigned in a match guard.

### コード例 (Example)
```rust
let mut x = Some(0);
match x {
    None => {}
    Some(_) if { x = None; false } => {} // error!
    Some(_) => {}
}
```

---

## 269. E0511 

### 概要 (English Summary)
Invalid monomorphization of an intrinsic function was used.

### 日本語解説・原因
`E0511` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Invalid monomorphization of an intrinsic function was used.

### コード例 (Example)
```rust
#![feature(intrinsics)]

#[rustc_intrinsic]
unsafe fn simd_add<T>(a: T, b: T) -> T;

fn main() {
    unsafe { simd_add(0, 1); }
    // error: invalid monomorphization of `simd_add` intrinsic
}
```

---

## 270. E0512 

### 概要 (English Summary)
Transmute with two differently sized types was attempted.

### 日本語解説・原因
`E0512` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Transmute with two differently sized types was attempted.

### コード例 (Example)
```rust
fn takes_u8(_: u8) {}

fn main() {
    unsafe { takes_u8(::std::mem::transmute(0u16)); }
    // error: cannot transmute between types of different sizes,
    //        or dependently-sized types
}
```

---

## 271. E0514 

### 概要 (English Summary)
Dependency compiled with different version of `rustc`.

### 日本語解説・原因
`E0514` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Dependency compiled with different version of `rustc`.

### コード例 (Example)
```rust
// compiled with stable `rustc`

#[crate_type = "lib"]
```

---

## 272. E0515 

### 概要 (English Summary)
A reference to a local variable was returned.

### 日本語解説・原因
`E0515` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A reference to a local variable was returned.

### コード例 (Example)
```rust
fn get_dangling_reference() -> &'static i32 {
    let x = 0;
    &x
}
```

---

## 273. E0516 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `typeof` keyword is currently reserved but unimplemented.

### 日本語解説・原因
このエラーコード（`E0516`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn main() {
    let x: typeof(92) = 92;
}
```

---

## 274. E0517 

### 概要 (English Summary)
A `#[repr(..)]` attribute was placed on an unsupported item.

### 日本語解説・原因
`E0517` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A `#[repr(..)]` attribute was placed on an unsupported item.

### コード例 (Example)
```rust
#[repr(C)]
type Foo = u8;

#[repr(packed)]
enum Foo {Bar, Baz}

#[repr(u8)]
struct Foo {bar: bool, baz: bool}

#[repr(C)]
impl Foo {
    // ...
}
```

---

## 275. E0518 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An `#[inline(..)]` attribute was incorrectly placed on something other than a

### 日本語解説・原因
このエラーコード（`E0518`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[inline(always)]
struct Foo;

#[inline(never)]
impl Foo {
    // ...
}
```

---

## 276. E0519 

### 概要 (English Summary)
The current crate is indistinguishable from one of its dependencies, in terms

### 日本語解説・原因
`E0519` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The current crate is indistinguishable from one of its dependencies, in terms

### コード例 (Example)
```rust
#![crate_name = "a"]
#![crate_type = "lib"]

pub fn foo() {}
```

---

## 277. E0520 

### 概要 (English Summary)
A non-default implementation was already made on this type so it cannot be

### 日本語解説・原因
`E0520` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A non-default implementation was already made on this type so it cannot be

### コード例 (Example)
```rust
#![feature(specialization)]

trait SpaceLlama {
    fn fly(&self);
}

// applies to all T
impl<T> SpaceLlama for T {
    default fn fly(&self) {}
}

// non-default impl
// applies to all `Clone` T and overrides the previous impl
impl<T: Clone> SpaceLlama for T {
    fn fly(&self) {}
}

// since `i32` is clone, this conflicts with the previous implementation
impl SpaceLlama for i32 {
    default fn fly(&self) {}
    // error: item `fly` is provided by an `impl` that specializes
    //        another, but the item in the parent `impl` is not marked
    //        `default` and so it cannot be specialized.
}
```

---

## 278. E0521 

### 概要 (English Summary)
Borrowed data escapes outside of closure.

### 日本語解説・原因
`E0521` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Borrowed data escapes outside of closure.

### コード例 (Example)
```rust
let mut list: Vec<&str> = Vec::new();

let _add = |el: &str| {
    list.push(el); // error: `el` escapes the closure body here
};
```

---

## 279. E0522 

### 概要 (English Summary)
The lang attribute was used in an invalid context.

### 日本語解説・原因
`E0522` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The lang attribute was used in an invalid context.

### コード例 (Example)
```rust
#![feature(lang_items)]

#[lang = "cookie"]
fn cookie() -> ! { // error: definition of an unknown lang item: `cookie`
    loop {}
}
```

---

## 280. E0523 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The compiler found multiple library files with the requested crate name.

### 日本語解説・原因
このエラーコード（`E0523`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
// aux-build:crateresolve-1.rs
// aux-build:crateresolve-2.rs
// aux-build:crateresolve-3.rs

extern crate crateresolve;
//~^ ERROR multiple candidates for `rlib` dependency `crateresolve` found

fn main() {}
```

---

## 281. E0524 

### 概要 (English Summary)
A variable which requires unique access is being used in more than one closure

### 日本語解説・原因
`E0524` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A variable which requires unique access is being used in more than one closure

### コード例 (Example)
```rust
fn set(x: &mut isize) {
    *x += 4;
}

fn dragoooon(x: &mut isize) {
    let mut c1 = || set(x);
    let mut c2 = || set(x); // error!

    c2();
    c1();
}
```

---

## 282. E0525 

### 概要 (English Summary)
A closure was used but didn't implement the expected trait.

### 日本語解説・原因
`E0525` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A closure was used but didn't implement the expected trait.

### コード例 (Example)
```rust
struct X;

fn foo<T>(_: T) {}
fn bar<T: Fn(u32)>(_: T) {}

fn main() {
    let x = X;
    let closure = |_| foo(x); // error: expected a closure that implements
                              //        the `Fn` trait, but this closure only
                              //        implements `FnOnce`
    bar(closure);
}
```

---

## 283. E0527 

### 概要 (English Summary)
The number of elements in an array or slice pattern differed from the number of

### 日本語解説・原因
`E0527` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The number of elements in an array or slice pattern differed from the number of

### コード例 (Example)
```rust
let r = &[1, 2, 3, 4];
match r {
    &[a, b] => { // error: pattern requires 2 elements but array
                 //        has 4
        println!("a={}, b={}", a, b);
    }
}
```

---

## 284. E0528 

### 概要 (English Summary)
An array or slice pattern required more elements than were present in the

### 日本語解説・原因
`E0528` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An array or slice pattern required more elements than were present in the

### コード例 (Example)
```rust
let r = &[1, 2];
match r {
    &[a, b, c, rest @ ..] => { // error: pattern requires at least 3
                               //        elements but array has 2
        println!("a={}, b={}, c={} rest={:?}", a, b, c, rest);
    }
}
```

---

## 285. E0529 

### 概要 (English Summary)
An array or slice pattern was matched against some other type.

### 日本語解説・原因
`E0529` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An array or slice pattern was matched against some other type.

### コード例 (Example)
```rust
let r: f32 = 1.0;
match r {
    [a, b] => { // error: expected an array or slice, found `f32`
        println!("a={}, b={}", a, b);
    }
}
```

---

## 286. E0530 

### 概要 (English Summary)
A binding shadowed something it shouldn't.

### 日本語解説・原因
`E0530` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A binding shadowed something it shouldn't.

### コード例 (Example)
```rust
enum Enum {
    WithField(i32)
}

use Enum::*;
match WithField(1) {
    WithField => {} // error: missing (_)
}
```

---

## 287. E0531 

### 概要 (English Summary)
An unknown tuple struct/variant has been used.

### 日本語解説・原因
`E0531` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unknown tuple struct/variant has been used.

### コード例 (Example)
```rust
let Type(x) = Type(12); // error!
match Bar(12) {
    Bar(x) => {} // error!
    _ => {}
}
```

---

## 288. E0532 

### 概要 (English Summary)
Pattern arm did not match expected kind.

### 日本語解説・原因
`E0532` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Pattern arm did not match expected kind.

### コード例 (Example)
```rust
enum State {
    Succeeded,
    Failed(String),
}

fn print_on_failure(state: &State) {
    match *state {
        // error: expected unit struct, unit variant or constant, found tuple
        //        variant `State::Failed`
        State::Failed => println!("Failed"),
        _ => ()
    }
}
```

---

## 289. E0533 

### 概要 (English Summary)
An item which isn't a unit struct, a variant, nor a constant has been used as a

### 日本語解説・原因
`E0533` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An item which isn't a unit struct, a variant, nor a constant has been used as a

### コード例 (Example)
```rust
struct Tortoise;

impl Tortoise {
    fn turtle(&self) -> u32 { 0 }
}

match 0u32 {
    Tortoise::turtle => {} // Error!
    _ => {}
}
if let Tortoise::turtle = 0u32 {} // Same error!
```

---

## 290. E0534 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This is because it was too specific to the `inline` attribute.

### 日本語解説・原因
このエラーコード（`E0534`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[inline()] // error: expected one argument
pub fn something() {}

fn main() {}
```

---

## 291. E0535 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This is because it was too specific to the `inline` attribute.

### 日本語解説・原因
このエラーコード（`E0535`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[inline(unknown)] // error: invalid argument
pub fn something() {}

fn main() {}
```

---

## 292. E0536 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `not` cfg-predicate was malformed.

### 日本語解説・原因
このエラーコード（`E0536`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(doc_cfg)]
#[doc(cfg(not()))]
pub fn main() {

}
```

---

## 293. E0537 

### 概要 (English Summary)
An unknown predicate was used inside the `cfg` attribute.

### 日本語解説・原因
`E0537` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unknown predicate was used inside the `cfg` attribute.

### コード例 (Example)
```rust
#[cfg(unknown())] // error: invalid predicate `unknown`
pub fn something() {}

pub fn main() {}
```

---

## 294. E0538 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Attribute contains same meta item more than once.

### 日本語解説・原因
このエラーコード（`E0538`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[deprecated(
    since="1.0.0",
    note="First deprecation note.",
    note="Second deprecation note." // error: multiple same meta item
)]
fn deprecated_function() {}
```

---

## 295. E0539 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An invalid meta-item was used inside an attribute.

### 日本語解説・原因
このエラーコード（`E0539`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(staged_api)]
#![allow(internal_features)]
#![stable(since = "1.0.0", feature = "test")]

#[deprecated(note)] // error!
#[unstable(feature = "deprecated_fn", issue = "123")]
fn deprecated() {}

#[unstable(feature = "unstable_struct", issue)] // error!
struct Unstable;

#[rustc_const_unstable(feature)] // error!
const fn unstable_fn() {}

#[stable(feature = "stable_struct", since)] // error!
struct Stable;

#[rustc_const_stable(feature)] // error!
const fn stable_fn() {}
```

---

## 296. E0541 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An unknown meta item was used.

### 日本語解説・原因
このエラーコード（`E0541`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[deprecated(
    since="1.0.0",
    // error: unknown meta item
    reason="Example invalid meta item. Should be 'note'")
]
fn deprecated_function() {}
```

---

## 297. E0542 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `since` value is missing in a stability attribute.

### 日本語解説・原因
このエラーコード（`E0542`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(staged_api)]
#![allow(internal_features)]
#![stable(since = "1.0.0", feature = "test")]

#[stable(feature = "_stable_fn")] // invalid
fn _stable_fn() {}

#[rustc_const_stable(feature = "_stable_const_fn")] // invalid
const fn _stable_const_fn() {}

#[stable(feature = "_deprecated_fn", since = "0.1.0")]
#[deprecated(
    note = "explanation for deprecation"
)] // invalid
fn _deprecated_fn() {}
```

---

## 298. E0543 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `note` value is missing in a stability attribute.

### 日本語解説・原因
このエラーコード（`E0543`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(staged_api)]
#![allow(internal_features)]
#![stable(since = "1.0.0", feature = "test")]

#[stable(since = "0.1.0", feature = "_deprecated_fn")]
#[deprecated(
    since = "1.0.0"
)] // invalid
fn _deprecated_fn() {}
```

---

## 299. E0544 

### 概要 (English Summary)
Multiple stability attributes were declared on the same item.

### 日本語解説・原因
`E0544` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Multiple stability attributes were declared on the same item.

### コード例 (Example)
```rust
#![feature(staged_api)]
#![allow(internal_features)]
#![stable(since = "1.0.0", feature = "rust1")]

#[stable(feature = "rust1", since = "1.0.0")]
#[stable(feature = "test", since = "2.0.0")] // invalid
fn foo() {}
```

---

## 300. E0545 

### 概要 (English Summary)
The `issue` value is incorrect in a stability attribute.

### 日本語解説・原因
`E0545` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `issue` value is incorrect in a stability attribute.

### コード例 (Example)
```rust
#![feature(staged_api)]
#![allow(internal_features)]
#![stable(since = "1.0.0", feature = "test")]

#[unstable(feature = "_unstable_fn", issue = "0")] // invalid
fn _unstable_fn() {}

#[rustc_const_unstable(feature = "_unstable_const_fn", issue = "0")] // invalid
const fn _unstable_const_fn() {}
```

---

## 301. E0546 

### 概要 (English Summary)
The `feature` value is missing in a stability attribute.

### 日本語解説・原因
`E0546` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `feature` value is missing in a stability attribute.

### コード例 (Example)
```rust
#![feature(staged_api)]
#![allow(internal_features)]
#![stable(since = "1.0.0", feature = "test")]

#[unstable(issue = "none")] // invalid
fn unstable_fn() {}

#[stable(since = "1.0.0")] // invalid
fn stable_fn() {}
```

---

## 302. E0547 

### 概要 (English Summary)
The `issue` value is missing in a stability attribute.

### 日本語解説・原因
`E0547` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `issue` value is missing in a stability attribute.

### コード例 (Example)
```rust
#![feature(staged_api)]
#![allow(internal_features)]
#![stable(since = "1.0.0", feature = "test")]

#[unstable(feature = "_unstable_fn")] // invalid
fn _unstable_fn() {}

#[rustc_const_unstable(feature = "_unstable_const_fn")] // invalid
const fn _unstable_const_fn() {}
```

---

## 303. E0549 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A `deprecated` attribute wasn't paired with a `stable`/`unstable` attribute with

### 日本語解説・原因
このエラーコード（`E0549`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(staged_api)]
#![allow(internal_features)]
#![stable(since = "1.0.0", feature = "test")]

#[deprecated(
    since = "1.0.1",
    note = "explanation for deprecation"
)] // invalid
fn _deprecated_fn() {}
```

---

## 304. E0550 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
More than one `deprecated` attribute has been put on an item.

### 日本語解説・原因
このエラーコード（`E0550`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[deprecated(note = "because why not?")]
#[deprecated(note = "right?")] // error!
fn the_banished() {}
```

---

## 305. E0551 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An invalid meta-item was used inside an attribute.

### 日本語解説・原因
このエラーコード（`E0551`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#[deprecated(note)] // error!
fn i_am_deprecated() {}
```

---

## 306. E0552 

### 概要 (English Summary)
A unrecognized representation attribute was used.

### 日本語解説・原因
`E0552` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A unrecognized representation attribute was used.

### コード例 (Example)
```rust
#[repr(D)] // error: unrecognized representation hint
struct MyStruct {
    my_field: usize
}
```

---

## 307. E0554 

### 概要 (English Summary)
Feature attributes are only allowed on the nightly release channel. Stable or

### 日本語解説・原因
`E0554` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Feature attributes are only allowed on the nightly release channel. Stable or

### コード例 (Example)
```rust
#![feature(lang_items)] // error: `#![feature]` may not be used on the
                        //        stable release channel
```

---

## 308. E0556 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `feature` attribute was badly formed.

### 日本語解説・原因
このエラーコード（`E0556`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(foo_bar_baz, foo(bar), foo = "baz", foo)] // error!
#![feature] // error!
#![feature = "foo"] // error!
```

---

## 309. E0557 

### 概要 (English Summary)
A feature attribute named a feature that has been removed.

### 日本語解説・原因
`E0557` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A feature attribute named a feature that has been removed.

### コード例 (Example)
```rust
#![feature(managed_boxes)] // error: feature has been removed
```

---

## 310. E0559 

### 概要 (English Summary)
An unknown field was specified into an enum's structure variant.

### 日本語解説・原因
`E0559` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unknown field was specified into an enum's structure variant.

### コード例 (Example)
```rust
enum Field {
    Fool { x: u32 },
}

let s = Field::Fool { joke: 0 };
// error: struct variant `Field::Fool` has no field named `joke`
```

---

## 311. E0560 

### 概要 (English Summary)
An unknown field was specified into a structure.

### 日本語解説・原因
`E0560` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unknown field was specified into a structure.

### コード例 (Example)
```rust
struct Simba {
    mother: u32,
}

let s = Simba { mother: 1, father: 0 };
// error: structure `Simba` has no field named `father`
```

---

## 312. E0561 

### 概要 (English Summary)
A non-ident or non-wildcard pattern has been used as a parameter of a function

### 日本語解説・原因
`E0561` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A non-ident or non-wildcard pattern has been used as a parameter of a function

### コード例 (Example)
```rust
type A1 = fn(mut param: u8); // error!
type A2 = fn(&param: u32); // error!
```

---

## 313. E0562 

### 概要 (English Summary)
`impl Trait` is only allowed as a function return and argument type.

### 日本語解説・原因
`E0562` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `impl Trait` is only allowed as a function return and argument type.

### コード例 (Example)
```rust
fn main() {
    let count_to_ten: impl Iterator<Item=usize> = 0..10;
    // error: `impl Trait` not allowed outside of function and inherent method
    //        return types
    for i in count_to_ten {
        println!("{}", i);
    }
}
```

---

## 314. E0565 

### 概要 (English Summary)
A literal was used in a built-in attribute that doesn't support literals.

### 日本語解説・原因
`E0565` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A literal was used in a built-in attribute that doesn't support literals.

### コード例 (Example)
```rust
#[repr("C")] // error: meta item in `repr` must be an identifier
struct Repr {}

fn main() {}
```

---

## 315. E0566 

### 概要 (English Summary)
Conflicting representation hints have been used on a same item.

### 日本語解説・原因
`E0566` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Conflicting representation hints have been used on a same item.

### コード例 (Example)
```rust
#[repr(u32, u64)]
enum Repr { A }
```

---

## 316. E0567 

### 概要 (English Summary)
Generics have been used on an auto trait.

### 日本語解説・原因
`E0567` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Generics have been used on an auto trait.

### コード例 (Example)
```rust
#![feature(auto_traits)]

auto trait Generic<T> {} // error!
```

---

## 317. E0568 

### 概要 (English Summary)
A super trait has been added to an auto trait.

### 日本語解説・原因
`E0568` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A super trait has been added to an auto trait.

### コード例 (Example)
```rust
#![feature(auto_traits)]

auto trait Bound : Copy {} // error!

fn main() {}
```

---

## 318. E0569 

### 概要 (English Summary)
If an impl has a generic parameter with the `#[may_dangle]` attribute, then

### 日本語解説・原因
`E0569` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: If an impl has a generic parameter with the `#[may_dangle]` attribute, then

### コード例 (Example)
```rust
#![feature(dropck_eyepatch)]

struct Foo<X>(X);
impl<#[may_dangle] X> Drop for Foo<X> {
    fn drop(&mut self) { }
}
```

---

## 319. E0570 

### 概要 (English Summary)
The requested ABI is unsupported by the current target.

### 日本語解説・原因
`E0570` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The requested ABI is unsupported by the current target.

---

## 320. E0571 

### 概要 (English Summary)
A `break` statement with an argument appeared in a non-`loop` loop.

### 日本語解説・原因
`E0571` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A `break` statement with an argument appeared in a non-`loop` loop.

### コード例 (Example)
```rust
let result = while true {
    if satisfied(i) {
        break 2 * i; // error: `break` with value from a `while` loop
    }
    i += 1;
};
```

---

## 321. E0572 

### 概要 (English Summary)
A return statement was found outside of a function body.

### 日本語解説・原因
`E0572` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A return statement was found outside of a function body.

### コード例 (Example)
```rust
const FOO: u32 = return 0; // error: return statement outside of function body

fn main() {}
```

---

## 322. E0573 

### 概要 (English Summary)
Something other than a type has been used when one was expected.

### 日本語解説・原因
`E0573` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Something other than a type has been used when one was expected.

### コード例 (Example)
```rust
enum Dragon {
    Born,
}

fn oblivion() -> Dragon::Born { // error!
    Dragon::Born
}

const HOBBIT: u32 = 2;
impl HOBBIT {} // error!

enum Wizard {
    Gandalf,
    Saruman,
}

trait Isengard {
    fn wizard(_: Wizard::Saruman); // error!
}
```

---

## 323. E0574 

### 概要 (English Summary)
Something other than a struct, variant or union has been used when one was

### 日本語解説・原因
`E0574` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Something other than a struct, variant or union has been used when one was

### コード例 (Example)
```rust
mod mordor {}

let sauron = mordor { x: () }; // error!

enum Jak {
    Daxter { i: isize },
}

let eco = Jak::Daxter { i: 1 };
match eco {
    Jak { i } => {} // error!
}
```

---

## 324. E0575 

### 概要 (English Summary)
Something other than a type or an associated type was given.

### 日本語解説・原因
`E0575` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Something other than a type or an associated type was given.

### コード例 (Example)
```rust
enum Rick { Morty }

let _: <u8 as Rick>::Morty; // error!

trait Age {
    type Empire;
    fn Mythology() {}
}

impl Age for u8 {
    type Empire = u16;
}

let _: <u8 as Age>::Mythology; // error!
```

---

## 325. E0576 

### 概要 (English Summary)
An associated item wasn't found in the given type.

### 日本語解説・原因
`E0576` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An associated item wasn't found in the given type.

### コード例 (Example)
```rust
trait Hello {
    type Who;

    fn hello() -> <Self as Hello>::You; // error!
}
```

---

## 326. E0577 

### 概要 (English Summary)
Something other than a module was found in visibility scope.

### 日本語解説・原因
`E0577` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Something other than a module was found in visibility scope.

### コード例 (Example)
```rust
pub enum Sea {}

pub (in crate::Sea) struct Shark; // error!

fn main() {}
```

---

## 327. E0578 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A module cannot be found and therefore, the visibility cannot be determined.

### 日本語解説・原因
このエラーコード（`E0578`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
foo!();

pub (in ::Sea) struct Shark; // error!

fn main() {}
```

---

## 328. E0579 

### 概要 (English Summary)
A lower range wasn't less than the upper range.

### 日本語解説・原因
`E0579` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A lower range wasn't less than the upper range.

### コード例 (Example)
```rust
fn main() {
    match 5u32 {
        // This range is ok, albeit pointless.
        1..2 => {}
        // This range is empty, and the compiler can tell.
        5..5 => {} // error!
    }
}
```

---

## 329. E0580 

### 概要 (English Summary)
The `main` function was incorrectly declared.

### 日本語解説・原因
`E0580` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `main` function was incorrectly declared.

### コード例 (Example)
```rust
fn main(x: i32) { // error: main function has wrong type
    println!("{}", x);
}
```

---

## 330. E0581 

### 概要 (English Summary)
In a `fn` type, a lifetime appears only in the return type

### 日本語解説・原因
`E0581` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: In a `fn` type, a lifetime appears only in the return type

### コード例 (Example)
```rust
fn main() {
    // Here, `'a` appears only in the return type:
    let x: for<'a> fn() -> &'a i32;
}
```

---

## 331. E0582 

### 概要 (English Summary)
A lifetime is only present in an associated-type binding, and not in the input

### 日本語解説・原因
`E0582` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A lifetime is only present in an associated-type binding, and not in the input

### コード例 (Example)
```rust
fn bar<F>(t: F)
    // No type can satisfy this requirement, since `'a` does not
    // appear in any of the input types (here, `i32`):
    where F: for<'a> Fn(i32) -> Option<&'a i32>
{
}

fn main() { }
```

---

## 332. E0583 

### 概要 (English Summary)
A file wasn't found for an out-of-line module.

### 日本語解説・原因
`E0583` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A file wasn't found for an out-of-line module.

### コード例 (Example)
```rust
mod file_that_doesnt_exist; // error: file not found for module

fn main() {}
```

---

## 333. E0584 

### 概要 (English Summary)
A doc comment that is not attached to anything has been encountered.

### 日本語解説・原因
`E0584` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A doc comment that is not attached to anything has been encountered.

### コード例 (Example)
```rust
trait Island {
    fn lost();

    /// I'm lost!
}
```

---

## 334. E0585 

### 概要 (English Summary)
A documentation comment that doesn't document anything was found.

### 日本語解説・原因
`E0585` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A documentation comment that doesn't document anything was found.

### コード例 (Example)
```rust
fn main() {
    // The following doc comment will fail:
    /// This is a useless doc comment!
}
```

---

## 335. E0586 

### 概要 (English Summary)
An inclusive range was used with no end.

### 日本語解説・原因
`E0586` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An inclusive range was used with no end.

### コード例 (Example)
```rust
fn main() {
    let tmp = vec![0, 1, 2, 3, 4, 4, 3, 3, 2, 1];
    let x = &tmp[1..=]; // error: inclusive range was used with no end
}
```

---

## 336. E0587 

### 概要 (English Summary)
A type has both `packed` and `align` representation hints.

### 日本語解説・原因
`E0587` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type has both `packed` and `align` representation hints.

### コード例 (Example)
```rust
#[repr(packed, align(8))] // error!
struct Umbrella(i32);
```

---

## 337. E0588 

### 概要 (English Summary)
A type with `packed` representation hint has a field with `align`

### 日本語解説・原因
`E0588` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type with `packed` representation hint has a field with `align`

### コード例 (Example)
```rust
#[repr(align(16))]
struct Aligned(i32);

#[repr(packed)] // error!
struct Packed(Aligned);
```

---

## 338. E0589 

### 概要 (English Summary)
The value of `N` that was specified for `repr(align(N))` was not a power

### 日本語解説・原因
`E0589` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The value of `N` that was specified for `repr(align(N))` was not a power

### コード例 (Example)
```rust
#[repr(align(15))] // error: invalid `repr(align)` attribute: not a power of two
enum Foo {
    Bar(u64),
}
```

---

## 339. E0590 

### 概要 (English Summary)
`break` or `continue` keywords were used in a condition of a `while` loop

### 日本語解説・原因
`E0590` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `break` or `continue` keywords were used in a condition of a `while` loop

### コード例 (Example)
```rust
while break {}
```

---

## 340. E0591 

### 概要 (English Summary)
Per [RFC 401][rfc401], if you have a function declaration `foo`:

### 日本語解説・原因
`E0591` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Per [RFC 401][rfc401], if you have a function declaration `foo`:

### コード例 (Example)
```rust
struct S;

// For the purposes of this explanation, all of these
// different kinds of `fn` declarations are equivalent:

fn foo(x: S) { /* ... */ }
extern "C" {
    fn foo(x: S);
}
impl S {
    fn foo(self) { /* ... */ }
}
```

---

## 341. E0592 

### 概要 (English Summary)
This error occurs when you defined methods or associated functions with same

### 日本語解説・原因
`E0592` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error occurs when you defined methods or associated functions with same

### コード例 (Example)
```rust
struct Foo;

impl Foo {
    fn bar() {} // previous definition here
}

impl Foo {
    fn bar() {} // duplicate definition here
}
```

---

## 342. E0593 

### 概要 (English Summary)
You tried to supply an `Fn`-based type with an incorrect number of arguments

### 日本語解説・原因
`E0593` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: You tried to supply an `Fn`-based type with an incorrect number of arguments

### コード例 (Example)
```rust
fn foo<F: Fn()>(x: F) { }

fn main() {
    // [E0593] closure takes 1 argument but 0 arguments are required
    foo(|y| { });
}
```

---

## 343. E0594 

### 概要 (English Summary)
A non-mutable value was assigned a value.

### 日本語解説・原因
`E0594` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A non-mutable value was assigned a value.

### コード例 (Example)
```rust
struct SolarSystem {
    earth: i32,
}

let ss = SolarSystem { earth: 3 };
ss.earth = 2; // error!
```

---

## 344. E0595 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Closures cannot mutate immutable captured variables.

### 日本語解説・原因
このエラーコード（`E0595`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
let x = 3; // error: closure cannot assign to immutable local variable `x`
let mut c = || { x += 1 };
```

---

## 345. E0596 

### 概要 (English Summary)
This error occurs because you tried to mutably borrow a non-mutable variable.

### 日本語解説・原因
`E0596` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error occurs because you tried to mutably borrow a non-mutable variable.

### コード例 (Example)
```rust
let x = 1;
let y = &mut x; // error: cannot borrow mutably
```

---

## 346. E0597 

### 概要 (English Summary)
This error occurs because a value was dropped while it was still borrowed.

### 日本語解説・原因
`E0597` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error occurs because a value was dropped while it was still borrowed.

### コード例 (Example)
```rust
struct Foo<'a> {
    x: Option<&'a u32>,
}

let mut x = Foo { x: None };
{
    let y = 0;
    x.x = Some(&y); // error: `y` does not live long enough
}
println!("{:?}", x.x);
```

---

## 347. E0599 

### 概要 (English Summary)
This error occurs when a method is used on a type which doesn't implement it:

### 日本語解説・原因
`E0599` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error occurs when a method is used on a type which doesn't implement it:

### コード例 (Example)
```rust
struct Mouth;

let x = Mouth;
x.chocolate(); // error: no method named `chocolate` found for type `Mouth`
               //        in the current scope
```

---

## 348. E0600 

### 概要 (English Summary)
An unary operator was used on a type which doesn't implement it.

### 日本語解説・原因
`E0600` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unary operator was used on a type which doesn't implement it.

### コード例 (Example)
```rust
enum Question {
    Yes,
    No,
}

!Question::Yes; // error: cannot apply unary operator `!` to type `Question`
```

---

## 349. E0601 

### 概要 (English Summary)
No `main` function was found in a binary crate.

### 日本語解説・原因
`E0601` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: No `main` function was found in a binary crate.

### コード例 (Example)
```rust
fn main() {
    // Your program will start here.
    println!("Hello world!");
}
```

---

## 350. E0602 

### 概要 (English Summary)
An unknown or invalid lint was used on the command line.

### 日本語解説・原因
`E0602` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unknown or invalid lint was used on the command line.

### コード例 (Example)
```rust
rustc -D bogus rust_file.rs
```

---

## 351. E0603 

### 概要 (English Summary)
A private item was used outside its scope.

### 日本語解説・原因
`E0603` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A private item was used outside its scope.

### コード例 (Example)
```rust
mod foo {
    const PRIVATE: u32 = 0x_a_bad_1dea_u32; // This const is private, so we
                                            // can't use it outside of the
                                            // `foo` module.
}

println!("const value: {}", foo::PRIVATE); // error: constant `PRIVATE`
                                                  //        is private
```

---

## 352. E0604 

### 概要 (English Summary)
A cast to `char` was attempted on a type other than `u8`.

### 日本語解説・原因
`E0604` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A cast to `char` was attempted on a type other than `u8`.

### コード例 (Example)
```rust
0u32 as char; // error: only `u8` can be cast as `char`, not `u32`
```

---

## 353. E0605 

### 概要 (English Summary)
An invalid cast was attempted.

### 日本語解説・原因
`E0605` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An invalid cast was attempted.

### コード例 (Example)
```rust
let x = 0u8;
x as Vec<u8>; // error: non-primitive cast: `u8` as `std::vec::Vec<u8>`

// Another example

let v = core::ptr::null::<u8>(); // So here, `v` is a `*const u8`.
v as &u8; // error: non-primitive cast: `*const u8` as `&u8`
```

---

## 354. E0606 

### 概要 (English Summary)
An incompatible cast was attempted.

### 日本語解説・原因
`E0606` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An incompatible cast was attempted.

### コード例 (Example)
```rust
let x = &0u8; // Here, `x` is a `&u8`.
let y: u32 = x as u32; // error: casting `&u8` as `u32` is invalid
```

---

## 355. E0607 

### 概要 (English Summary)
A cast between a thin and a wide pointer was attempted.

### 日本語解説・原因
`E0607` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A cast between a thin and a wide pointer was attempted.

### コード例 (Example)
```rust
let v = core::ptr::null::<u8>();
v as *const [u8];
```

---

## 356. E0608 

### 概要 (English Summary)
Attempted to index a value whose type doesn't implement the

### 日本語解説・原因
`E0608` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Attempted to index a value whose type doesn't implement the

### コード例 (Example)
```rust
0u8[2]; // error: cannot index into a value of type `u8`
```

---

## 357. E0609 

### 概要 (English Summary)
Attempted to access a nonexistent field in a struct.

### 日本語解説・原因
`E0609` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Attempted to access a nonexistent field in a struct.

### コード例 (Example)
```rust
struct StructWithFields {
    x: u32,
}

let s = StructWithFields { x: 0 };
println!("{}", s.foo); // error: no field `foo` on type `StructWithFields`
```

---

## 358. E0610 

### 概要 (English Summary)
Attempted to access a field on a primitive type.

### 日本語解説・原因
`E0610` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Attempted to access a field on a primitive type.

### コード例 (Example)
```rust
let x: u32 = 0;
println!("{}", x.foo); // error: `{integer}` is a primitive type, therefore
                       //        doesn't have fields
```

---

## 359. E0614 

### 概要 (English Summary)
Attempted to dereference a variable which cannot be dereferenced.

### 日本語解説・原因
`E0614` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Attempted to dereference a variable which cannot be dereferenced.

### コード例 (Example)
```rust
let y = 0u32;
*y; // error: type `u32` cannot be dereferenced
```

---

## 360. E0615 

### 概要 (English Summary)
Attempted to access a method like a field.

### 日本語解説・原因
`E0615` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Attempted to access a method like a field.

### コード例 (Example)
```rust
struct Foo {
    x: u32,
}

impl Foo {
    fn method(&self) {}
}

let f = Foo { x: 0 };
f.method; // error: attempted to take value of method `method` on type `Foo`
```

---

## 361. E0616 

### 概要 (English Summary)
Attempted to access a private field on a struct.

### 日本語解説・原因
`E0616` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Attempted to access a private field on a struct.

### コード例 (Example)
```rust
mod some_module {
    pub struct Foo {
        x: u32, // So `x` is private in here.
    }

    impl Foo {
        pub fn new() -> Foo { Foo { x: 0 } }
    }
}

let f = some_module::Foo::new();
println!("{}", f.x); // error: field `x` of struct `some_module::Foo` is private
```

---

## 362. E0617 

### 概要 (English Summary)
Attempted to pass an invalid type of variable into a variadic function.

### 日本語解説・原因
`E0617` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Attempted to pass an invalid type of variable into a variadic function.

### コード例 (Example)
```rust
extern "C" {
    fn printf(format: *const c_char, ...) -> c_int;
}

unsafe {
    printf("%f\n\0".as_ptr() as _, 0f32);
    // error: cannot pass an `f32` to variadic function, cast to `c_double`
}
```

---

## 363. E0618 

### 概要 (English Summary)
Attempted to call something which isn't a function nor a method.

### 日本語解説・原因
`E0618` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Attempted to call something which isn't a function nor a method.

### コード例 (Example)
```rust
enum X {
    Entry,
}

X::Entry(); // error: expected function, tuple struct or tuple variant,
            // found `X::Entry`

// Or even simpler:
let x = 0i32;
x(); // error: expected function, tuple struct or tuple variant, found `i32`
```

---

## 364. E0619 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The type-checker needed to know the type of an expression, but that type had not

### 日本語解説・原因
このエラーコード（`E0619`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
let mut x = vec![];
match x.pop() {
    Some(v) => {
        // Here, the type of `v` is not (yet) known, so we
        // cannot resolve this method call:
        v.to_uppercase(); // error: the type of this value must be known in
                          //        this context
    }
    None => {}
}
```

---

## 365. E0620 

### 概要 (English Summary)
A cast to an unsized type was attempted.

### 日本語解説・原因
`E0620` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A cast to an unsized type was attempted.

### コード例 (Example)
```rust
let x = &[1_usize, 2] as [usize]; // error: cast to unsized type: `&[usize; 2]`
                                  //        as `[usize]`
```

---

## 366. E0621 

### 概要 (English Summary)
This error code indicates a mismatch between the lifetimes appearing in the

### 日本語解説・原因
`E0621` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error code indicates a mismatch between the lifetimes appearing in the

### コード例 (Example)
```rust
fn foo<'a>(x: &'a i32, y: &i32) -> &'a i32 { // error: explicit lifetime
                                             //        required in the type of
                                             //        `y`
    if x > y { x } else { y }
}
```

---

## 367. E0622 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An intrinsic was declared without being a function.

### 日本語解説・原因
このエラーコード（`E0622`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(intrinsics)]
#![allow(internal_features)]

extern "C" {
    #[rustc_intrinsic]
    pub static atomic_singlethreadfence_seqcst: unsafe fn();
    // error: intrinsic must be a function
}

fn main() { unsafe { atomic_singlethreadfence_seqcst(); } }
```

---

## 368. E0623 

### 概要 (English Summary)
A lifetime didn't match what was expected.

### 日本語解説・原因
`E0623` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A lifetime didn't match what was expected.

### コード例 (Example)
```rust
struct Foo<'a, 'b, T>(std::marker::PhantomData<(&'a (), &'b (), T)>)
where
    T: Convert<'a, 'b>;

trait Convert<'a, 'b>: Sized {
    fn cast(&'a self) -> &'b Self;
}
impl<'long: 'short, 'short, T> Convert<'long, 'short> for T {
    fn cast(&'long self) -> &'short T {
        self
    }
}
// error
fn badboi<'in_, 'out, T>(
    x: Foo<'in_, 'out, T>,
    sadness: &'in_ T
) -> &'out T {
    sadness.cast()
}
```

---

## 369. E0624 

### 概要 (English Summary)
A private item was used outside of its scope.

### 日本語解説・原因
`E0624` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A private item was used outside of its scope.

### コード例 (Example)
```rust
mod inner {
    pub struct Foo;

    impl Foo {
        fn method(&self) {}
    }
}

let foo = inner::Foo;
foo.method(); // error: method `method` is private
```

---

## 370. E0625 

### 概要 (English Summary)
A compile-time const variable is referring to a thread-local static variable.

### 日本語解説・原因
`E0625` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A compile-time const variable is referring to a thread-local static variable.

### コード例 (Example)
```rust
#![feature(thread_local)]

#[thread_local]
static X: usize = 12;

const Y: usize = 2 * X;
```

---

## 371. E0626 

### 概要 (English Summary)
This error occurs because a borrow in a movable coroutine persists across a

### 日本語解説・原因
`E0626` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error occurs because a borrow in a movable coroutine persists across a

### コード例 (Example)
```rust
let mut b = #[coroutine] || {
    let a = &String::new(); // <-- This borrow...
    yield (); // ...is still in scope here, when the yield occurs.
    println!("{}", a);
};
Pin::new(&mut b).resume(());
```

---

## 372. E0627 

### 概要 (English Summary)
A yield expression was used outside of the coroutine literal.

### 日本語解説・原因
`E0627` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A yield expression was used outside of the coroutine literal.

### コード例 (Example)
```rust
#![feature(coroutines, coroutine_trait, stmt_expr_attributes)]

fn fake_coroutine() -> &'static str {
    yield 1;
    return "foo"
}

fn main() {
    let mut coroutine = fake_coroutine;
}
```

---

## 373. E0628 

### 概要 (English Summary)
More than one parameter was used for a coroutine.

### 日本語解説・原因
`E0628` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: More than one parameter was used for a coroutine.

### コード例 (Example)
```rust
#![feature(coroutines, coroutine_trait, stmt_expr_attributes)]

fn main() {
    let coroutine = #[coroutine] |a: i32, b: i32| {
        // error: too many parameters for a coroutine
        // Allowed only 0 or 1 parameter
        yield a;
    };
}
```

---

## 374. E0631 

### 概要 (English Summary)
This error indicates a type mismatch in closure arguments.

### 日本語解説・原因
`E0631` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error indicates a type mismatch in closure arguments.

### コード例 (Example)
```rust
fn foo<F: Fn(i32)>(f: F) {
}

fn main() {
    foo(|x: &str| {});
}
```

---

## 375. E0632 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An explicit generic argument was provided when calling a function that

### 日本語解説・原因
このエラーコード（`E0632`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn foo<T: Copy>(a: T, b: impl Clone) {}

foo::<i32>(0i32, "abc".to_string());
```

---

## 376. E0633 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `unwind` attribute was malformed.

### 日本語解説・原因
このエラーコード（`E0633`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(unwind_attributes)]

#[unwind()] // error: expected one argument
pub extern "C" fn something() {}

fn main() {}
```

---

## 377. E0634 

### 概要 (English Summary)
A type has conflicting `packed` representation hints.

### 日本語解説・原因
`E0634` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type has conflicting `packed` representation hints.

### コード例 (Example)
```rust
#[repr(packed, packed(2))] // error!
struct Company(i32);

#[repr(packed(2))] // error!
#[repr(packed)]
struct Company(i32);
```

---

## 378. E0635 

### 概要 (English Summary)
The `#![feature]` attribute specified an unknown feature.

### 日本語解説・原因
`E0635` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `#![feature]` attribute specified an unknown feature.

### コード例 (Example)
```rust
#![feature(nonexistent_rust_feature)] // error: unknown feature
```

---

## 379. E0636 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The same feature is enabled multiple times with `#![feature]` attributes

### 日本語解説・原因
このエラーコード（`E0636`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![allow(stable_features)]
#![feature(rust1)]
#![feature(rust1)] // error: the feature `rust1` has already been enabled
```

---

## 380. E0637 

### 概要 (English Summary)
`'_` lifetime name or `&T` without an explicit lifetime name has been used

### 日本語解説・原因
`E0637` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `'_` lifetime name or `&T` without an explicit lifetime name has been used

### コード例 (Example)
```rust
fn underscore_lifetime<'_>(str1: &'_ str, str2: &'_ str) -> &'_ str {
                     //^^ `'_` is a reserved lifetime name
    if str1.len() > str2.len() {
        str1
    } else {
        str2
    }
}

fn without_explicit_lifetime<T>()
where
    T: Iterator<Item = &u32>,
                     //^ `&` without an explicit lifetime name
{
}

fn without_hrtb<T>()
where
    T: Into<&u32>,
          //^ `&` without an explicit lifetime name
{
}
```

---

## 381. E0638 

### 概要 (English Summary)
This error indicates that the struct, enum or enum variant must be matched

### 日本語解説・原因
`E0638` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error indicates that the struct, enum or enum variant must be matched

### コード例 (Example)
```rust
#[non_exhaustive]
pub enum Error {
    Message(String),
    Other,
}

impl Display for Error {
    fn fmt(&self, formatter: &mut fmt::Formatter) -> fmt::Result {
        // This will not error, despite being marked as non_exhaustive, as this
        // enum is defined within the current crate, it can be matched
        // exhaustively.
        let display = match self {
            Message(s) => s,
            Other => "other or unknown error",
        };
        formatter.write_str(display)
    }
}
```

---

## 382. E0639 

### 概要 (English Summary)
This error indicates that the struct, enum or enum variant cannot be

### 日本語解説・原因
`E0639` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error indicates that the struct, enum or enum variant cannot be

### コード例 (Example)
```rust
#[non_exhaustive]
pub struct NormalStruct {
    pub first_field: u16,
    pub second_field: u16,
}

let ns = NormalStruct { first_field: 640, second_field: 480 }; // error!
```

---

## 383. E0640 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
詳細な説明はありません。

### 日本語解説・原因
このエラーコード（`E0640`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 384. E0641 

### 概要 (English Summary)
Attempted to cast to/from a pointer with an unknown kind.

### 日本語解説・原因
`E0641` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Attempted to cast to/from a pointer with an unknown kind.

### コード例 (Example)
```rust
let b = 0 as *const _; // error
```

---

## 385. E0642 

### 概要 (English Summary)
Trait methods currently cannot take patterns as arguments.

### 日本語解説・原因
`E0642` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Trait methods currently cannot take patterns as arguments.

### コード例 (Example)
```rust
trait Foo {
    fn foo((x, y): (i32, i32)); // error: patterns aren't allowed
                                //        in trait methods
}
```

---

## 386. E0643 

### 概要 (English Summary)
This error indicates that there is a mismatch between generic parameters and

### 日本語解説・原因
`E0643` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error indicates that there is a mismatch between generic parameters and

### コード例 (Example)
```rust
trait Foo {
    fn foo(&self, _: &impl Iterator);
}
impl Foo for () {
    fn foo<U: Iterator>(&self, _: &U) { } // error method `foo` has incompatible
                                          // signature for trait
}
```

---

## 387. E0644 

### 概要 (English Summary)
A closure or generator was constructed that references its own type.

### 日本語解説・原因
`E0644` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A closure or generator was constructed that references its own type.

### コード例 (Example)
```rust
fn fix<F>(f: &F)
  where F: Fn(&F)
{
    f(&f);
}

fn main() {
    fix(&|y| {
        // Here, when `x` is called, the parameter `y` is equal to `x`.
    });
}
```

---

## 388. E0646 

### 概要 (English Summary)
It is not possible to define `main` with a where clause.

### 日本語解説・原因
`E0646` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: It is not possible to define `main` with a where clause.

### コード例 (Example)
```rust
fn main() where i32: Copy { // error: main function is not allowed to have
                            // a where clause
}
```

---

## 389. E0647 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `start` function was defined with a where clause.

### 日本語解説・原因
このエラーコード（`E0647`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 390. E0648 

### 概要 (English Summary)
An `export_name` attribute contains null characters (`\0`).

### 日本語解説・原因
`E0648` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An `export_name` attribute contains null characters (`\0`).

### コード例 (Example)
```rust
#[export_name="\0foo"] // error: `export_name` may not contain null characters
pub fn bar() {}
```

---

## 391. E0657 

### 概要 (English Summary)
An `impl Trait` captured a higher-ranked lifetime, which is not supported.

### 日本語解説・原因
`E0657` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An `impl Trait` captured a higher-ranked lifetime, which is not supported.

### コード例 (Example)
```rust
trait BorrowInto<'a> {
    type Target;

    fn borrow_into(&'a self) -> Self::Target;
}

impl<'a> BorrowInto<'a> for () {
    type Target = &'a ();

    fn borrow_into(&'a self) -> Self::Target {
        self
    }
}

fn opaque() -> impl for<'a> BorrowInto<'a, Target = impl Sized + 'a> {
    ()
}
```

---

## 392. E0658 

### 概要 (English Summary)
An unstable feature was used.

### 日本語解説・原因
`E0658` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unstable feature was used.

### コード例 (Example)
```rust
use std::intrinsics; // error: use of unstable library feature `core_intrinsics`
```

---

## 393. E0659 

### 概要 (English Summary)
An item usage is ambiguous.

### 日本語解説・原因
`E0659` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An item usage is ambiguous.

### コード例 (Example)
```rust
pub mod moon {
    pub fn foo() {}
}

pub mod earth {
    pub fn foo() {}
}

mod collider {
    pub use crate::moon::*;
    pub use crate::earth::*;
}

fn main() {
    crate::collider::foo(); // ERROR: `foo` is ambiguous
}
```

---

## 394. E0660 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The argument to the `llvm_asm` macro is not well-formed.

### 日本語解説・原因
このエラーコード（`E0660`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
llvm_asm!("nop" "nop");
```

---

## 395. E0661 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An invalid syntax was passed to the second argument of an `llvm_asm` macro line.

### 日本語解説・原因
このエラーコード（`E0661`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
let a;
llvm_asm!("nop" : "r"(a));
```

---

## 396. E0662 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An invalid input operand constraint was passed to the `llvm_asm` macro

### 日本語解説・原因
このエラーコード（`E0662`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
llvm_asm!("xor %eax, %eax"
          :
          : "=test"("a")
         );
```

---

## 397. E0663 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An invalid input operand constraint was passed to the `llvm_asm` macro

### 日本語解説・原因
このエラーコード（`E0663`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
llvm_asm!("xor %eax, %eax"
          :
          : "+test"("a")
         );
```

---

## 398. E0664 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A clobber was surrounded by braces in the `llvm_asm` macro.

### 日本語解説・原因
このエラーコード（`E0664`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
llvm_asm!("mov $$0x200, %eax"
          :
          :
          : "{eax}"
         );
```

---

## 399. E0665 

### 概要 (English Summary)
The `Default` trait was derived on an enum without specifying the default

### 日本語解説・原因
`E0665` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `Default` trait was derived on an enum without specifying the default

### コード例 (Example)
```rust
#[derive(Default)]
enum Food {
    Sweet,
    Salty,
}
```

---

## 400. E0666 

### 概要 (English Summary)
`impl Trait` types cannot appear nested in the generic arguments of other

### 日本語解説・原因
`E0666` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `impl Trait` types cannot appear nested in the generic arguments of other

### コード例 (Example)
```rust
trait MyGenericTrait<T> {}
trait MyInnerTrait {}

fn foo(
    bar: impl MyGenericTrait<impl MyInnerTrait>, // error!
) {}
```

---

