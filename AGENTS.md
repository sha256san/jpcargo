# AGENTS.md - AI エージェント向け開発ガイドライン & ルール

本リポジトリで作業する AI エージェント（Claude, Gemini, ChatGPT 等）は、以下の規約・設計原則を厳格に遵守してください。

---

## 1. 開発の基本原則（7箇条）

1. **rustc の診断を最優先する (rustc is the Source of Truth)**:
   - AI が独自にコンパイルエラーの真偽を決めつけたり、rustc の判定を曲げてはならない。
   - コンパイラが提供する診断情報（型、借用期間、エラーコード）を正確に尊重する。

2. **直訳・機械翻訳にしない (Explain, Don't Just Translate)**:
   - 単なる英語から日本語への単語変換ではなく、「なぜエラーになったか」「Rust のどの仕組みが関わっているか」を初心者に分かるように解説する。

3. **コンテキスト情報を失わない (Preserve Error Code & Location)**:
   - エラーコード（例: `E0596`）、ファイルパス、行・列番号、スニペット情報は絶対に欠落させない。

4. **原文確認を可能にする (Keep Original Accessible)**:
   - ユーザーが希望した際（`--original` やフォールバック時）に、コンパイラ原文を参照できる設計を維持する。

5. **未対応エラーでも停止しない (Graceful Fallback)**:
   - 日本語ルールが未実装のエラーコードが来ても panic やツール停止を起こさず、綺麗なフォールバック表示を行う。

6. **Cargo の透過的互換性を壊さない (Transparent Cargo Wrapper)**:
   - Cargo のフラグや引数は極力そのまま受け渡し、通常のビルドフローを阻害しない。

7. **高品質なターミナル出力を維持する (Rich Terminal Aesthetics)**:
   - 色分け、ボックス線、インデントを整え、一目で問題が把握できるデザインにする。

---

## 2. 新規エラーコードの追加手順

新しいエラーコード（例: `E0xxx`）の日本語診断を追加する場合は、以下の手順に従ってください。

1. **ルールファイルの作成**:
   - `src/japanese/rules/eXXXX.rs` を新規作成する。
   - `DiagnosticRule` トレイトを実装し、概要、原因、修正方法、コード例を定義する。
2. **ルールモジュールの登録**:
   - `src/japanese/rules/mod.rs` にモジュールを追加し、ディスパッチャーに追加する。
3. **動的コンテキスト抽出**:
   - rustc のメッセージやスニペットから変数名や型名を正規表現などで安全に抽出し、プレースホルダーに差し込む。
4. **辞書・ドキュメントの更新**:
   - `TODO.md` の対応状況テーブルを更新する。
   - `dictionary/rust_terms.json` に新しい用語があれば追加する。
5. **テストの追加**:
   - 該当エラーコードのユニットテストを `tests/` またはインラインテストとして追加し、`cargo test` が通ることを確認する。

---

## 3. コーディング規約

- **Rust Edition**: 2021 Edition
- **命名規則**: Rust 標準の命名規則（`snake_case` for functions/modules, `CamelCase` for structs/enums, `SCREAMING_SNAKE_CASE` for constants）に準拠する。
- **エラーハンドリング**: `unwrap()` の多用を避け、`Result` や `Option` を適切にハンドリングしてフォールバックを用意する。
- **外部依存の選定**: 依存クレートは最小限かつ信頼性の高いもの（`clap`, `serde`, `colored` 等）に留める。
- **ドキュメントの同期**: 仕様やコードの変更を行った際は、`TODO.md`, `MEMORY.md`, `CHANGELOG.md` を随時更新する。
