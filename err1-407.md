# Rust コンパイラエラー一覧 (1〜200: E0001 〜 E0407)
> 出典: [Rust Error Codes Index](https://doc.rust-lang.org/error_codes/error-index.html)
> 本ドキュメントは `jpcargo` 開発用エラーデータベースとして、公式ドキュメントおよび rustc 診断情報に基づき整理・日本語化したものです。
---

## 1. E0001 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This error suggests that the expression arm corresponding to the noted pattern

### 日本語解説・原因
このエラーコード（`E0001`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
match Some(0) {
    Some(bar) => {/* ... */}
    x => {/* ... */} // This handles the `None` case
    _ => {/* ... */} // All possible cases have already been handled
}
```

---

## 2. E0002 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This error indicates that an empty match expression is invalid because the type

### 日本語解説・原因
このエラーコード（`E0002`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
enum Empty {}

fn foo(x: Empty) {
    match x {
        // empty
    }
}
```

---

## 3. E0004 

### 概要 (English Summary)
This error indicates that the compiler cannot guarantee a matching pattern for

### 日本語解説・原因
`E0004` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error indicates that the compiler cannot guarantee a matching pattern for

### コード例 (Example)
```rust
enum Terminator {
    HastaLaVistaBaby,
    TalkToMyHand,
}

let x = Terminator::HastaLaVistaBaby;

match x { // error: non-exhaustive patterns: `HastaLaVistaBaby` not covered
    Terminator::TalkToMyHand => {}
}
```

---

## 4. E0005 

### 概要 (English Summary)
Patterns used to bind names must be irrefutable, that is, they must guarantee

### 日本語解説・原因
`E0005` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Patterns used to bind names must be irrefutable, that is, they must guarantee

### コード例 (Example)
```rust
let x = Some(1);
let Some(y) = x;
// error: refutable pattern in local binding: `None` not covered
```

---

## 5. E0007 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This error indicates that the bindings in a match arm would require a value to

### 日本語解説・原因
このエラーコード（`E0007`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(bindings_after_at)]

let x = Some("s".to_string());

match x {
    op_string @ Some(s) => {}, // error: use of moved value
    None => {},
}
```

---

## 6. E0009 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
In a pattern, all values that don't implement the `Copy` trait have to be bound

### 日本語解説・原因
このエラーコード（`E0009`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(move_ref_pattern)]

struct X { x: (), }

let x = Some((X { x: () }, X { x: () }));
match x {
    Some((y, ref z)) => {}, // error: cannot bind by-move and by-ref in the
                            //        same pattern
    None => panic!()
}
```

---

## 7. E0010 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The value of statics and constants must be known at compile time, and they live

### 日本語解説・原因
このエラーコード（`E0010`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
const CON : Vec<i32> = vec![1, 2, 3];
```

---

## 8. E0013 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Static and const variables can refer to other const variables. But a const

### 日本語解説・原因
このエラーコード（`E0013`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
static X: i32 = 42;
const Y: i32 = X;
```

---

## 9. E0014 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Constants can only be initialized by a constant value or, in a future

### 日本語解説・原因
このエラーコード（`E0014`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
const FOO: i32 = { let x = 0; x }; // 'x' isn't a constant nor a function!
```

---

## 10. E0015 

### 概要 (English Summary)
A non-`const` function was called in a `const` context.

### 日本語解説・原因
`E0015` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A non-`const` function was called in a `const` context.

### コード例 (Example)
```rust
fn create_some() -> Option<u8> {
    Some(1)
}

// error: cannot call non-const function `create_some` in constants
const FOO: Option<u8> = create_some();
```

---

## 11. E0023 

### 概要 (English Summary)
A pattern attempted to extract an incorrect number of fields from a variant.

### 日本語解説・原因
`E0023` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A pattern attempted to extract an incorrect number of fields from a variant.

### コード例 (Example)
```rust
enum Fruit {
    Apple(String, String),
    Pear(u32),
}

let x = Fruit::Apple(String::new(), String::new());

match x {
    Fruit::Apple(a) => {}, // error!
    _ => {}
}
```

---

## 12. E0025 

### 概要 (English Summary)
Each field of a struct can only be bound once in a pattern.

### 日本語解説・原因
`E0025` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Each field of a struct can only be bound once in a pattern.

### コード例 (Example)
```rust
struct Foo {
    a: u8,
    b: u8,
}

fn main(){
    let x = Foo { a:1, b:2 };

    let Foo { a: x, a: y } = x;
    // error: field `a` bound multiple times in the pattern
}
```

---

## 13. E0026 

### 概要 (English Summary)
A struct pattern attempted to extract a nonexistent field from a struct.

### 日本語解説・原因
`E0026` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A struct pattern attempted to extract a nonexistent field from a struct.

### コード例 (Example)
```rust
struct Thing {
    x: u32,
    y: u32,
}

let thing = Thing { x: 0, y: 0 };

match thing {
    Thing { x, z } => {} // error: `Thing::z` field doesn't exist
}
```

---

## 14. E0027 

### 概要 (English Summary)
A pattern for a struct fails to specify a sub-pattern for every one of the

### 日本語解説・原因
`E0027` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A pattern for a struct fails to specify a sub-pattern for every one of the

### コード例 (Example)
```rust
struct Dog {
    name: String,
    age: u32,
}

let d = Dog { name: "Rusty".to_string(), age: 8 };

// This is incorrect.
match d {
    Dog { age: x } => {}
}
```

---

## 15. E0029 

### 概要 (English Summary)
Something other than numbers and characters has been used for a range.

### 日本語解説・原因
`E0029` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Something other than numbers and characters has been used for a range.

### コード例 (Example)
```rust
let string = "salutations !";

// The ordering relation for strings cannot be evaluated at compile time,
// so this doesn't work:
match string {
    "hello" ..= "world" => {}
    _ => {}
}

// This is a more general version, using a guard:
match string {
    s if s >= "hello" && s <= "world" => {}
    _ => {}
}
```

---

## 16. E0030 

### 概要 (English Summary)
When matching against a range, the compiler verifies that the range is

### 日本語解説・原因
`E0030` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: When matching against a range, the compiler verifies that the range is

### コード例 (Example)
```rust
match 5u32 {
    // This range is ok, albeit pointless.
    1 ..= 1 => {}
    // This range is empty, and the compiler can tell.
    1000 ..= 5 => {}
}
```

---

## 17. E0033 

### 概要 (English Summary)
A trait type has been dereferenced.

### 日本語解説・原因
`E0033` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A trait type has been dereferenced.

### コード例 (Example)
```rust
let trait_obj: &SomeTrait = &"some_value";

// This tries to implicitly dereference to create an unsized local variable.
let &invalid = trait_obj;

// You can call methods without binding to the value being pointed at.
trait_obj.method_one();
trait_obj.method_two();
```

---

## 18. E0034 

### 概要 (English Summary)
The compiler doesn't know what method to call because more than one method

### 日本語解説・原因
`E0034` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The compiler doesn't know what method to call because more than one method

### コード例 (Example)
```rust
struct Test;

trait Trait1 {
    fn foo();
}

trait Trait2 {
    fn foo();
}

impl Trait1 for Test { fn foo() {} }
impl Trait2 for Test { fn foo() {} }

fn main() {
    Test::foo() // error, which foo() to call?
}
```

---

## 19. E0038 

### 概要 (English Summary)
For any given trait `Trait` there may be a related _type_ called the _trait

### 日本語解説・原因
`E0038` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: For any given trait `Trait` there may be a related _type_ called the _trait

### コード例 (Example)
```rust
trait Trait {
}

fn static_foo<T:Trait + ?Sized>(b: &T) {
}

fn dynamic_bar(a: &dyn Trait) {
    static_foo(a)
}
```

---

## 20. E0040 

### 概要 (English Summary)
It is not allowed to manually call destructors in Rust.

### 日本語解説・原因
`E0040` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: It is not allowed to manually call destructors in Rust.

### コード例 (Example)
```rust
struct Foo {
    x: i32,
}

impl Drop for Foo {
    fn drop(&mut self) {
        println!("kaboom");
    }
}

fn main() {
    let mut x = Foo { x: -7 };
    x.drop(); // error: explicit use of destructor method
}
```

---

## 21. E0044 

### 概要 (English Summary)
You cannot use type or const parameters on foreign items.

### 日本語解説・原因
`E0044` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: You cannot use type or const parameters on foreign items.

### コード例 (Example)
```rust
extern "C" { fn some_func<T>(x: T); }
```

---

## 22. E0045 

### 概要 (English Summary)
Variadic parameters have been used on a non-C ABI function.

### 日本語解説・原因
`E0045` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Variadic parameters have been used on a non-C ABI function.

### コード例 (Example)
```rust
extern "Rust" {
    fn foo(x: u8, ...); // error!
}
```

---

## 23. E0046 

### 概要 (English Summary)
Items are missing in a trait implementation.

### 日本語解説・原因
`E0046` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Items are missing in a trait implementation.

### コード例 (Example)
```rust
trait Foo {
    fn foo();
}

struct Bar;

impl Foo for Bar {}
// error: not all trait items implemented, missing: `foo`
```

---

## 24. E0049 

### 概要 (English Summary)
An attempted implementation of a trait method has the wrong number of type or

### 日本語解説・原因
`E0049` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An attempted implementation of a trait method has the wrong number of type or

### コード例 (Example)
```rust
trait Foo {
    fn foo<T: Default>(x: T) -> Self;
}

struct Bar;

// error: method `foo` has 0 type parameters but its trait declaration has 1
// type parameter
impl Foo for Bar {
    fn foo(x: bool) -> Self { Bar }
}
```

---

## 25. E0050 

### 概要 (English Summary)
An attempted implementation of a trait method has the wrong number of function

### 日本語解説・原因
`E0050` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An attempted implementation of a trait method has the wrong number of function

### コード例 (Example)
```rust
trait Foo {
    fn foo(&self, x: u8) -> bool;
}

struct Bar;

// error: method `foo` has 1 parameter but the declaration in trait `Foo::foo`
// has 2
impl Foo for Bar {
    fn foo(&self) -> bool { true }
}
```

---

## 26. E0053 

### 概要 (English Summary)
The parameters of any trait method must match between a trait implementation

### 日本語解説・原因
`E0053` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The parameters of any trait method must match between a trait implementation

### コード例 (Example)
```rust
trait Foo {
    fn foo(x: u16);
    fn bar(&self);
}

struct Bar;

impl Foo for Bar {
    // error, expected u16, found i16
    fn foo(x: i16) { }

    // error, types differ in mutability
    fn bar(&mut self) { }
}
```

---

## 27. E0054 

### 概要 (English Summary)
It is not allowed to cast to a bool.

### 日本語解説・原因
`E0054` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: It is not allowed to cast to a bool.

### コード例 (Example)
```rust
let x = 5;

// Not allowed, won't compile
let x_is_nonzero = x as bool;
```

---

## 28. E0055 

### 概要 (English Summary)
During a method call, a value is automatically dereferenced as many times as

### 日本語解説・原因
`E0055` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: During a method call, a value is automatically dereferenced as many times as

### コード例 (Example)
```rust
#![recursion_limit="4"]

struct Foo;

impl Foo {
    fn foo(&self) {}
}

fn main() {
    let foo = Foo;
    let ref_foo = &&&&&Foo;

    // error, reached the recursion limit while auto-dereferencing `&&&&&Foo`
    ref_foo.foo();
}
```

---

## 29. E0057 

### 概要 (English Summary)
An invalid number of arguments was given when calling a closure.

### 日本語解説・原因
`E0057` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An invalid number of arguments was given when calling a closure.

### コード例 (Example)
```rust
let f = |x| x * 3;
let a = f();        // invalid, too few parameters
let b = f(4);       // this works!
let c = f(2, 3);    // invalid, too many parameters
```

---

## 30. E0059 

### 概要 (English Summary)
The built-in function traits are generic over a tuple of the function arguments.

### 日本語解説・原因
`E0059` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The built-in function traits are generic over a tuple of the function arguments.

### コード例 (Example)
```rust
#![feature(unboxed_closures)]

fn foo<F: Fn<i32>>(f: F) -> F::Output { f(3) }
```

---

## 31. E0060 

### 概要 (English Summary)
External C functions are allowed to be variadic. However, a variadic function

### 日本語解説・原因
`E0060` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: External C functions are allowed to be variadic. However, a variadic function

### コード例 (Example)
```rust
use std::os::raw::{c_char, c_int};

extern "C" {
    fn printf(_: *const c_char, ...) -> c_int;
}

unsafe { printf(); } // error!
```

---

## 32. E0061 

### 概要 (English Summary)
An invalid number of arguments was passed when calling a function.

### 日本語解説・原因
`E0061` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An invalid number of arguments was passed when calling a function.

### コード例 (Example)
```rust
fn f(u: i32) {}

f(); // error!
```

---

## 33. E0062 

### 概要 (English Summary)
A struct's or struct-like enum variant's field was specified more than once.

### 日本語解説・原因
`E0062` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A struct's or struct-like enum variant's field was specified more than once.

### コード例 (Example)
```rust
struct Foo {
    x: i32,
}

fn main() {
    let x = Foo {
                x: 0,
                x: 0, // error: field `x` specified more than once
            };
}
```

---

## 34. E0063 

### 概要 (English Summary)
A struct's or struct-like enum variant's field was not provided.

### 日本語解説・原因
`E0063` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A struct's or struct-like enum variant's field was not provided.

### コード例 (Example)
```rust
struct Foo {
    x: i32,
    y: i32,
}

fn main() {
    let x = Foo { x: 0 }; // error: missing field: `y`
}
```

---

## 35. E0067 

### 概要 (English Summary)
An invalid left-hand side expression was used on an assignment operation.

### 日本語解説・原因
`E0067` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An invalid left-hand side expression was used on an assignment operation.

### コード例 (Example)
```rust
12 += 1; // error!
```

---

## 36. E0069 

### 概要 (English Summary)
The compiler found a function whose body contains a `return;` statement but

### 日本語解説・原因
`E0069` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The compiler found a function whose body contains a `return;` statement but

### コード例 (Example)
```rust
// error
fn foo() -> u8 {
    return;
}
```

---

## 37. E0070 

### 概要 (English Summary)
An assignment operator was used on a non-place expression.

### 日本語解説・原因
`E0070` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An assignment operator was used on a non-place expression.

### コード例 (Example)
```rust
struct SomeStruct {
    x: i32,
    y: i32,
}

const SOME_CONST: i32 = 12;

fn some_other_func() {}

fn some_function() {
    SOME_CONST = 14; // error: a constant value cannot be changed!
    1 = 3; // error: 1 isn't a valid place!
    some_other_func() = 4; // error: we cannot assign value to a function!
    SomeStruct::x = 12; // error: SomeStruct a structure name but it is used
                        //        like a variable!
}
```

---

## 38. E0071 

### 概要 (English Summary)
A structure-literal syntax was used to create an item that is not a structure

### 日本語解説・原因
`E0071` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A structure-literal syntax was used to create an item that is not a structure

### コード例 (Example)
```rust
type U32 = u32;
let t = U32 { value: 4 }; // error: expected struct, variant or union type,
                          // found builtin type `u32`
```

---

## 39. E0072 

### 概要 (English Summary)
A recursive type has infinite size because it doesn't have an indirection.

### 日本語解説・原因
`E0072` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A recursive type has infinite size because it doesn't have an indirection.

### コード例 (Example)
```rust
struct ListNode {
    head: u8,
    tail: Option<ListNode>, // error: no indirection here so impossible to
                            //        compute the type's size
}
```

---

## 40. E0073 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
You cannot define a struct (or enum) `Foo` that requires an instance of `Foo`

### 日本語解説・原因
このエラーコード（`E0073`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
struct Foo { x: Box<Foo> } // error
```

---

## 41. E0074 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
When using the `#[simd]` attribute on a tuple struct, the components of the

### 日本語解説・原因
このエラーコード（`E0074`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(repr_simd)]

#[repr(simd)]
struct Bad<T>([T; 4]);
```

---

## 42. E0075 

### 概要 (English Summary)
A `#[simd]` attribute was applied to an empty or multi-field struct.

### 日本語解説・原因
`E0075` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A `#[simd]` attribute was applied to an empty or multi-field struct.

### コード例 (Example)
```rust
#![feature(repr_simd)]

#[repr(simd)]
struct Bad; // error!
```

---

## 43. E0076 

### 概要 (English Summary)
The type of the field in a tuple struct isn't an array when using the `#[simd]`

### 日本語解説・原因
`E0076` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The type of the field in a tuple struct isn't an array when using the `#[simd]`

### コード例 (Example)
```rust
#![feature(repr_simd)]

#[repr(simd)]
struct Bad(u16); // error!
```

---

## 44. E0077 

### 概要 (English Summary)
A tuple struct's element isn't a machine type when using the `#[simd]`

### 日本語解説・原因
`E0077` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A tuple struct's element isn't a machine type when using the `#[simd]`

### コード例 (Example)
```rust
#![feature(repr_simd)]

#[repr(simd)]
struct Bad([String; 2]); // error!
```

---

## 45. E0080 

### 概要 (English Summary)
A constant value failed to get evaluated.

### 日本語解説・原因
`E0080` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A constant value failed to get evaluated.

### コード例 (Example)
```rust
enum Enum {
    X = (1 << 500),
    Y = (1 / 0),
}
```

---

## 46. E0081 

### 概要 (English Summary)
A discriminant value is present more than once.

### 日本語解説・原因
`E0081` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A discriminant value is present more than once.

### コード例 (Example)
```rust
enum Enum {
    P = 3,
    X = 3, // error!
    Y = 5,
}
```

---

## 47. E0084 

### 概要 (English Summary)
An unsupported representation was attempted on a zero-variant enum.

### 日本語解説・原因
`E0084` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unsupported representation was attempted on a zero-variant enum.

### コード例 (Example)
```rust
#[repr(i32)]
enum NightsWatch {} // error: unsupported representation for zero-variant enum
```

---

## 48. E0087 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Too many type arguments were supplied for a function. For example:

### 日本語解説・原因
このエラーコード（`E0087`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn foo<T>() {}

fn main() {
    foo::<f64, bool>(); // error: wrong number of type arguments:
                        //        expected 1, found 2
}
```

---

## 49. E0088 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
You gave too many lifetime arguments. Erroneous code example:

### 日本語解説・原因
このエラーコード（`E0088`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn f() {}

fn main() {
    f::<'static>() // error: wrong number of lifetime arguments:
                   //        expected 0, found 1
}
```

---

## 50. E0089 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Too few type arguments were supplied for a function. For example:

### 日本語解説・原因
このエラーコード（`E0089`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn foo<T, U>() {}

fn main() {
    foo::<f64>(); // error: wrong number of type arguments: expected 2, found 1
}
```

---

## 51. E0090 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
You gave too few lifetime arguments. Example:

### 日本語解説・原因
このエラーコード（`E0090`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn foo<'a: 'b, 'b: 'a>() {}

fn main() {
    foo::<'static>(); // error: wrong number of lifetime arguments:
                      //        expected 2, found 1
}
```

---

## 52. E0091 

### 概要 (English Summary)
An unnecessary type parameter was given in a type alias.

### 日本語解説・原因
`E0091` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unnecessary type parameter was given in a type alias.

### コード例 (Example)
```rust
type Foo<T> = u32; // error: type parameter `T` is never used
// or:
type Foo<A, B> = Box<A>; // error: type parameter `B` is never used
```

---

## 53. E0092 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An undefined atomic operation function was declared.

### 日本語解説・原因
このエラーコード（`E0092`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(intrinsics)]
#![allow(internal_features)]

#[rustc_intrinsic]
unsafe fn atomic_foo(); // error: unrecognized atomic operation
                        //        function
```

---

## 54. E0093 

### 概要 (English Summary)
An unknown intrinsic function was declared.

### 日本語解説・原因
`E0093` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unknown intrinsic function was declared.

### コード例 (Example)
```rust
#![feature(intrinsics)]
#![allow(internal_features)]

#[rustc_intrinsic]
unsafe fn foo(); // error: unrecognized intrinsic function: `foo`

fn main() {
    unsafe {
        foo();
    }
}
```

---

## 55. E0094 

### 概要 (English Summary)
An invalid number of generic parameters was passed to an intrinsic function.

### 日本語解説・原因
`E0094` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An invalid number of generic parameters was passed to an intrinsic function.

### コード例 (Example)
```rust
#![feature(intrinsics)]
#![allow(internal_features)]

#[rustc_intrinsic]
fn size_of<T, U>() -> usize; // error: intrinsic has wrong number
                             //        of type parameters
```

---

## 56. E0106 

### 概要 (English Summary)
This error indicates that a lifetime is missing from a type. If it is an error

### 日本語解説・原因
`E0106` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error indicates that a lifetime is missing from a type. If it is an error

### コード例 (Example)
```rust
struct Foo1 { x: &bool }
              // ^ expected lifetime parameter
struct Foo2<'a> { x: &'a bool } // correct

struct Bar1 { x: Foo2 }
              // ^^^^ expected lifetime parameter
struct Bar2<'a> { x: Foo2<'a> } // correct

enum Baz1 { A(u8), B(&bool), }
                  // ^ expected lifetime parameter
enum Baz2<'a> { A(u8), B(&'a bool), } // correct

type MyStr1 = &str;
           // ^ expected lifetime parameter
type MyStr2<'a> = &'a str; // correct
```

---

## 57. E0107 

### 概要 (English Summary)
An incorrect number of generic arguments was provided.

### 日本語解説・原因
`E0107` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An incorrect number of generic arguments was provided.

### コード例 (Example)
```rust
struct Foo<T> { x: T }

struct Bar { x: Foo }             // error: wrong number of type arguments:
                                  //        expected 1, found 0
struct Baz<S, T> { x: Foo<S, T> } // error: wrong number of type arguments:
                                  //        expected 1, found 2

fn foo<T, U>(x: T, y: U) {}
fn f() {}

fn main() {
    let x: bool = true;
    foo::<bool>(x);                 // error: wrong number of type arguments:
                                    //        expected 2, found 1
    foo::<bool, i32, i32>(x, 2, 4); // error: wrong number of type arguments:
                                    //        expected 2, found 3
    f::<'static>();                 // error: wrong number of lifetime arguments
                                    //        expected 0, found 1
}
```

---

## 58. E0109 

### 概要 (English Summary)
You tried to provide a generic argument to a type which doesn't need it.

### 日本語解説・原因
`E0109` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: You tried to provide a generic argument to a type which doesn't need it.

### コード例 (Example)
```rust
type X = u32<i32>; // error: type arguments are not allowed for this type
type Y = bool<'static>; // error: lifetime parameters are not allowed on
                        //        this type
```

---

## 59. E0110 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
You tried to provide a lifetime to a type which doesn't need it.

### 日本語解説・原因
このエラーコード（`E0110`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 60. E0116 

### 概要 (English Summary)
An inherent implementation was defined for a type outside the current crate.

### 日本語解説・原因
`E0116` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An inherent implementation was defined for a type outside the current crate.

### コード例 (Example)
```rust
impl Vec<u8> { } // error
```

---

## 61. E0117 

### 概要 (English Summary)
Only traits defined in the current crate can be implemented for arbitrary types.

### 日本語解説・原因
`E0117` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Only traits defined in the current crate can be implemented for arbitrary types.

### コード例 (Example)
```rust
impl Drop for u32 {}
```

---

## 62. E0118 

### 概要 (English Summary)
An inherent implementation was defined for something which isn't a struct,

### 日本語解説・原因
`E0118` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An inherent implementation was defined for something which isn't a struct,

### コード例 (Example)
```rust
impl<T> T { // error: no nominal type found for inherent implementation
    fn get_state(&self) -> String {
        // ...
    }
}
```

---

## 63. E0119 

### 概要 (English Summary)
There are conflicting trait implementations for the same type.

### 日本語解説・原因
`E0119` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: There are conflicting trait implementations for the same type.

### コード例 (Example)
```rust
trait MyTrait {
    fn get(&self) -> usize;
}

impl<T> MyTrait for T {
    fn get(&self) -> usize { 0 }
}

struct Foo {
    value: usize
}

impl MyTrait for Foo { // error: conflicting implementations of trait
                       //        `MyTrait` for type `Foo`
    fn get(&self) -> usize { self.value }
}
```

---

## 64. E0120 

### 概要 (English Summary)
`Drop` was implemented on a trait object or reference, which is not allowed;

### 日本語解説・原因
`E0120` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `Drop` was implemented on a trait object or reference, which is not allowed;

### コード例 (Example)
```rust
trait MyTrait {}

impl Drop for MyTrait {
    fn drop(&mut self) {}
}
```

---

## 65. E0121 

### 概要 (English Summary)
The type placeholder `_` was used within a type on an item's signature.

### 日本語解説・原因
`E0121` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The type placeholder `_` was used within a type on an item's signature.

### コード例 (Example)
```rust
fn foo() -> _ { 5 } // error

static BAR: _ = "test"; // error
```

---

## 66. E0124 

### 概要 (English Summary)
A struct was declared with two fields having the same name.

### 日本語解説・原因
`E0124` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A struct was declared with two fields having the same name.

### コード例 (Example)
```rust
struct Foo {
    field1: i32,
    field1: i32, // error: field is already declared
}
```

---

## 67. E0128 

### 概要 (English Summary)
A type parameter with default value is using forward declared identifier.

### 日本語解説・原因
`E0128` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type parameter with default value is using forward declared identifier.

### コード例 (Example)
```rust
struct Foo<T = U, U = ()> {
    field1: T,
    field2: U,
}
// error: generic parameters with a default cannot use forward declared
//        identifiers
```

---

## 68. E0130 

### 概要 (English Summary)
A pattern was declared as an argument in a foreign function declaration.

### 日本語解説・原因
`E0130` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A pattern was declared as an argument in a foreign function declaration.

### コード例 (Example)
```rust
extern "C" {
    fn foo((a, b): (u32, u32)); // error: patterns aren't allowed in foreign
                                //        function declarations
}
```

---

## 69. E0131 

### 概要 (English Summary)
The `main` function was defined with generic parameters.

### 日本語解説・原因
`E0131` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `main` function was defined with generic parameters.

### コード例 (Example)
```rust
fn main<T>() { // error: main function is not allowed to have generic parameters
}
```

---

## 70. E0132 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A function with the `start` attribute was declared with type parameters.

### 日本語解説・原因
このエラーコード（`E0132`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 71. E0133 

### 概要 (English Summary)
Unsafe code was used outside of an unsafe block.

### 日本語解説・原因
`E0133` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Unsafe code was used outside of an unsafe block.

### コード例 (Example)
```rust
unsafe fn f() { return; } // This is the unsafe code

fn main() {
    f(); // error: call to unsafe function requires unsafe function or block
}
```

---

## 72. E0136 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
More than one `main` function was found.

### 日本語解説・原因
このエラーコード（`E0136`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn main() {
    // ...
}

// ...

fn main() { // error!
    // ...
}
```

---

## 73. E0137 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
More than one function was declared with the `#[main]` attribute.

### 日本語解説・原因
このエラーコード（`E0137`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(main)]

#[main]
fn foo() {}

#[main]
fn f() {} // error: multiple functions with a `#[main]` attribute
```

---

## 74. E0138 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
More than one function was declared with the `#[start]` attribute.

### 日本語解説・原因
このエラーコード（`E0138`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 75. E0139 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
There are various restrictions on transmuting between types in Rust; for example

### 日本語解説・原因
このエラーコード（`E0139`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
use std::mem::transmute;

struct Foo<T>(Vec<T>);

fn foo<T>(x: Vec<T>) {
    // we are transmuting between Vec<T> and Foo<F> here
    let y: Foo<T> = unsafe { transmute(x) };
    // do something with y
}
```

---

## 76. E0152 

### 概要 (English Summary)
A lang item was redefined.

### 日本語解説・原因
`E0152` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A lang item was redefined.

### コード例 (Example)
```rust
#![feature(lang_items)]

#[lang = "owned_box"]
struct Foo<T>(T); // error: duplicate lang item found: `owned_box`
```

---

## 77. E0154 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Imports (`use` statements) are not allowed after non-item statements, such as

### 日本語解説・原因
このエラーコード（`E0154`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn f() {
    // Variable declaration before import
    let x = 0;
    use std::io::Read;
    // ...
}
```

---

## 78. E0158 

### 概要 (English Summary)
A generic parameter or `static` has been referenced in a pattern.

### 日本語解説・原因
`E0158` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A generic parameter or `static` has been referenced in a pattern.

### コード例 (Example)
```rust
enum Foo {
    One,
    Two
}

trait Bar {
    const X: Foo;
}

fn test<A: Bar>(arg: Foo) {
    match arg {
        A::X => println!("A::X"), // error: E0158: constant pattern depends
                                  //        on a generic parameter
        Foo::Two => println!("Two")
    }
}
```

---

## 79. E0161 

### 概要 (English Summary)
A value was moved whose size was not known at compile time.

### 日本語解説・原因
`E0161` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A value was moved whose size was not known at compile time.

### コード例 (Example)
```rust
trait Bar {
    fn f(self);
}

impl Bar for i32 {
    fn f(self) {}
}

fn main() {
    let b: Box<dyn Bar> = Box::new(0i32);
    b.f();
    // error: cannot move a value of type dyn Bar: the size of dyn Bar cannot
    //        be statically determined
}
```

---

## 80. E0162 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An `if let` pattern attempts to match the pattern, and enters the body if the

### 日本語解説・原因
このエラーコード（`E0162`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
struct Irrefutable(i32);
let irr = Irrefutable(0);

// This fails to compile because the match is irrefutable.
if let Irrefutable(x) = irr {
    // This body will always be executed.
    // ...
}
```

---

## 81. E0164 

### 概要 (English Summary)
Something which is neither a tuple struct nor a tuple variant was used as a

### 日本語解説・原因
`E0164` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Something which is neither a tuple struct nor a tuple variant was used as a

### コード例 (Example)
```rust
enum A {
    B,
    C,
}

impl A {
    fn new() {}
}

fn bar(foo: A) {
    match foo {
        A::new() => (), // error!
        _ => {}
    }
}
```

---

## 82. E0165 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A `while let` pattern attempts to match the pattern, and enters the body if the

### 日本語解説・原因
このエラーコード（`E0165`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
struct Irrefutable(i32);
let irr = Irrefutable(0);

// This fails to compile because the match is irrefutable.
while let Irrefutable(x) = irr {
    // ...
}
```

---

## 83. E0170 

### 概要 (English Summary)
A pattern binding is using the same name as one of the variants of a type.

### 日本語解説・原因
`E0170` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A pattern binding is using the same name as one of the variants of a type.

### コード例 (Example)
```rust
enum Method {
    GET,
    POST,
}

fn is_empty(s: Method) -> bool {
    match s {
        GET => true,
        _ => false
    }
}

fn main() {}
```

---

## 84. E0178 

### 概要 (English Summary)
The `+` type operator was used in an ambiguous context.

### 日本語解説・原因
`E0178` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `+` type operator was used in an ambiguous context.

### コード例 (Example)
```rust
trait Foo {}

struct Bar<'a> {
    x: &'a Foo + 'a,     // error!
    y: &'a mut Foo + 'a, // error!
    z: fn() -> Foo + 'a, // error!
}
```

---

## 85. E0183 

### 概要 (English Summary)
Manual implementation of a `Fn*` trait.

### 日本語解説・原因
`E0183` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Manual implementation of a `Fn*` trait.

### コード例 (Example)
```rust
struct MyClosure {
    foo: i32
}

impl FnOnce<()> for MyClosure {  // error
    type Output = ();
    extern "rust-call" fn call_once(self, args: ()) -> Self::Output {
        println!("{}", self.foo);
    }
}
```

---

## 86. E0184 

### 概要 (English Summary)
The `Copy` trait was implemented on a type with a `Drop` implementation.

### 日本語解説・原因
`E0184` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `Copy` trait was implemented on a type with a `Drop` implementation.

### コード例 (Example)
```rust
#[derive(Copy)]
struct Foo; // error!

impl Drop for Foo {
    fn drop(&mut self) {
    }
}
```

---

## 87. E0185 

### 概要 (English Summary)
An associated function for a trait was defined to be static, but an

### 日本語解説・原因
`E0185` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An associated function for a trait was defined to be static, but an

### コード例 (Example)
```rust
trait Foo {
    fn foo();
}

struct Bar;

impl Foo for Bar {
    // error, method `foo` has a `&self` declaration in the impl, but not in
    // the trait
    fn foo(&self) {}
}
```

---

## 88. E0186 

### 概要 (English Summary)
An associated function for a trait was defined to be a method (i.e., to take a

### 日本語解説・原因
`E0186` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An associated function for a trait was defined to be a method (i.e., to take a

### コード例 (Example)
```rust
trait Foo {
    fn foo(&self);
}

struct Bar;

impl Foo for Bar {
    // error, method `foo` has a `&self` declaration in the trait, but not in
    // the impl
    fn foo() {}
}
```

---

## 89. E0191 

### 概要 (English Summary)
An associated type wasn't specified for a trait object.

### 日本語解説・原因
`E0191` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An associated type wasn't specified for a trait object.

### コード例 (Example)
```rust
trait Trait {
    type Bar;
}

type Foo = dyn Trait; // error: the value of the associated type `Bar` (from
                      //        the trait `Trait`) must be specified
```

---

## 90. E0192 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A negative impl was added on a trait implementation.

### 日本語解説・原因
このエラーコード（`E0192`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
trait Trait {
    type Bar;
}

struct Foo;

impl !Trait for Foo { } //~ ERROR

fn main() {}
```

---

## 91. E0193 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
`where` clauses must use generic type parameters: it does not make sense to use

### 日本語解説・原因
このエラーコード（`E0193`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
trait Foo {
    fn bar(&self);
}

#[derive(Copy,Clone)]
struct Wrapper<T> {
    Wrapped: T
}

impl Foo for Wrapper<u32> where Wrapper<u32>: Clone {
    fn bar(&self) { }
}
```

---

## 92. E0195 

### 概要 (English Summary)
The lifetime parameters of the method do not match the trait declaration.

### 日本語解説・原因
`E0195` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The lifetime parameters of the method do not match the trait declaration.

### コード例 (Example)
```rust
trait Trait {
    fn bar<'a,'b:'a>(x: &'a str, y: &'b str);
}

struct Foo;

impl Trait for Foo {
    fn bar<'a,'b>(x: &'a str, y: &'b str) {
    // error: lifetime parameters or bounds on method `bar`
    // do not match the trait declaration
    }
}
```

---

## 93. E0197 

### 概要 (English Summary)
An inherent implementation was marked unsafe.

### 日本語解説・原因
`E0197` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An inherent implementation was marked unsafe.

### コード例 (Example)
```rust
struct Foo;

unsafe impl Foo { } // error!
```

---

## 94. E0198 

### 概要 (English Summary)
A negative implementation was marked as unsafe.

### 日本語解説・原因
`E0198` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A negative implementation was marked as unsafe.

### コード例 (Example)
```rust
struct Foo;

unsafe impl !Clone for Foo { } // error!
```

---

## 95. E0199 

### 概要 (English Summary)
A trait implementation was marked as unsafe while the trait is safe.

### 日本語解説・原因
`E0199` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A trait implementation was marked as unsafe while the trait is safe.

### コード例 (Example)
```rust
struct Foo;

trait Bar { }

unsafe impl Bar for Foo { } // error!
```

---

## 96. E0200 

### 概要 (English Summary)
An unsafe trait was implemented without an unsafe implementation.

### 日本語解説・原因
`E0200` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unsafe trait was implemented without an unsafe implementation.

### コード例 (Example)
```rust
struct Foo;

unsafe trait Bar { }

impl Bar for Foo { } // error!
```

---

## 97. E0201 

### 概要 (English Summary)
Two associated items (like methods, associated types, associated functions,

### 日本語解説・原因
`E0201` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Two associated items (like methods, associated types, associated functions,

### コード例 (Example)
```rust
struct Foo(u8);

impl Foo {
    fn bar(&self) -> bool { self.0 > 5 }
    fn bar() {} // error: duplicate associated function
}

trait Baz {
    type Quux;
    fn baz(&self) -> bool;
}

impl Baz for Foo {
    type Quux = u32;

    fn baz(&self) -> bool { true }

    // error: duplicate method
    fn baz(&self) -> bool { self.0 > 5 }

    // error: duplicate associated type
    type Quux = u32;
}
```

---

## 98. E0203 

### 概要 (English Summary)
Having duplicate relaxed default bounds is unsupported.

### 日本語解説・原因
`E0203` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Having duplicate relaxed default bounds is unsupported.

### コード例 (Example)
```rust
struct Bad<T: ?Sized + ?Sized>{
    inner: T,
}
```

---

## 99. E0204 

### 概要 (English Summary)
The `Copy` trait was implemented on a type which contains a field that doesn't

### 日本語解説・原因
`E0204` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `Copy` trait was implemented on a type which contains a field that doesn't

### コード例 (Example)
```rust
struct Foo {
    foo: Vec<u32>,
}

impl Copy for Foo { } // error!
```

---

## 100. E0205 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An attempt to implement the `Copy` trait for an enum failed because one of the

### 日本語解説・原因
このエラーコード（`E0205`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
enum Foo {
    Bar(Vec<u32>),
    Baz,
}

impl Copy for Foo { }
```

---

## 101. E0206 

### 概要 (English Summary)
The `Copy` trait was implemented on a type which is neither a struct, an

### 日本語解説・原因
`E0206` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `Copy` trait was implemented on a type which is neither a struct, an

### コード例 (Example)
```rust
#[derive(Copy, Clone)]
struct Bar;

impl Copy for &'static mut Bar { } // error!
```

---

## 102. E0207 

### 概要 (English Summary)
A type, const or lifetime parameter that is specified for `impl` is not

### 日本語解説・原因
`E0207` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type, const or lifetime parameter that is specified for `impl` is not

### コード例 (Example)
```rust
struct Foo;

impl<T: Default> Foo {
    // error: the type parameter `T` is not constrained by the impl trait, self
    // type, or predicates [E0207]
    fn get(&self) -> T {
        <T as Default>::default()
    }
}
```

---

## 103. E0208 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
詳細な説明はありません。

### 日本語解説・原因
このエラーコード（`E0208`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 104. E0210 

### 概要 (English Summary)
This error indicates a violation of one of Rust's orphan rules for trait

### 日本語解説・原因
`E0210` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error indicates a violation of one of Rust's orphan rules for trait

### コード例 (Example)
```rust
extern crate foo;
use foo::ForeignTrait;

impl<T> ForeignTrait for T { } // error
```

---

## 105. E0211 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
You used a function or type which doesn't fit the requirements for where it was

### 日本語解説・原因
このエラーコード（`E0211`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(intrinsics)]
#![allow(internal_features)]

#[rustc_intrinsic]
unsafe fn unreachable(); // error: intrinsic has wrong type

// or:

fn main() -> i32 { 0 }
// error: main function expects type: `fn() {main}`: expected (), found i32

// or:

let x = 1u8;
match x {
    0u8..=3i8 => (),
    // error: mismatched types in range: expected u8, found i8
    _ => ()
}

// or:

use std::rc::Rc;
struct Foo;

impl Foo {
    fn x(self: Rc<Foo>) {}
    // error: mismatched self type: expected `Foo`: expected struct
    //        `Foo`, found struct `alloc::rc::Rc`
}
```

---

## 106. E0212 

### 概要 (English Summary)
Cannot use the associated type of

### 日本語解説・原因
`E0212` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Cannot use the associated type of

### コード例 (Example)
```rust
pub trait Foo<T> {
    type A;

    fn get(&self, t: T) -> Self::A;
}

fn foo2<I : for<'x> Foo<&'x isize>>(
    field: I::A) {} // error!
```

---

## 107. E0214 

### 概要 (English Summary)
A generic type was described using parentheses rather than angle brackets.

### 日本語解説・原因
`E0214` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A generic type was described using parentheses rather than angle brackets.

### コード例 (Example)
```rust
let v: Vec(&str) = vec!["foo"];
```

---

## 108. E0220 

### 概要 (English Summary)
The associated type used was not defined in the trait.

### 日本語解説・原因
`E0220` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The associated type used was not defined in the trait.

### コード例 (Example)
```rust
trait T1 {
    type Bar;
}

type Foo = T1<F=i32>; // error: associated type `F` not found for `T1`

// or:

trait T2 {
    type Bar;

    // error: Baz is used but not declared
    fn return_bool(&self, _: &Self::Bar, _: &Self::Baz) -> bool;
}
```

---

## 109. E0221 

### 概要 (English Summary)
An attempt was made to retrieve an associated type, but the type was ambiguous.

### 日本語解説・原因
`E0221` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An attempt was made to retrieve an associated type, but the type was ambiguous.

### コード例 (Example)
```rust
trait T1 {}
trait T2 {}

trait Foo {
    type A: T1;
}

trait Bar : Foo {
    type A: T2;
    fn do_something() {
        let _: Self::A;
    }
}
```

---

## 110. E0222 

### 概要 (English Summary)
An attempt was made to constrain an associated type.

### 日本語解説・原因
`E0222` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An attempt was made to constrain an associated type.

### コード例 (Example)
```rust
pub trait Vehicle {
    type Color;
}

pub trait Box {
    type Color;
}

pub trait BoxCar : Box + Vehicle {}

fn dent_object<COLOR>(c: dyn BoxCar<Color=COLOR>) {} // Invalid constraint
```

---

## 111. E0223 

### 概要 (English Summary)
An attempt was made to retrieve an associated type, but the type was ambiguous.

### 日本語解説・原因
`E0223` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An attempt was made to retrieve an associated type, but the type was ambiguous.

### コード例 (Example)
```rust
trait Trait { type X; }

fn main() {
    let foo: Trait::X;
}
```

---

## 112. E0224 

### 概要 (English Summary)
A trait object was declared with no traits.

### 日本語解説・原因
`E0224` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A trait object was declared with no traits.

### コード例 (Example)
```rust
type Foo = dyn 'static +;
```

---

## 113. E0225 

### 概要 (English Summary)
Multiple types were used as bounds for a closure or trait object.

### 日本語解説・原因
`E0225` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Multiple types were used as bounds for a closure or trait object.

### コード例 (Example)
```rust
fn main() {
    let _: Box<dyn std::io::Read + std::io::Write>;
}
```

---

## 114. E0226 

### 概要 (English Summary)
More than one explicit lifetime bound was used on a trait object.

### 日本語解説・原因
`E0226` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: More than one explicit lifetime bound was used on a trait object.

### コード例 (Example)
```rust
trait Foo {}

type T<'a, 'b> = dyn Foo + 'a + 'b; // error: Trait object `arg` has two
                                    //        lifetime bound, 'a and 'b.
```

---

## 115. E0227 

### 概要 (English Summary)
This error indicates that the compiler is unable to determine whether there is

### 日本語解説・原因
`E0227` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error indicates that the compiler is unable to determine whether there is

### コード例 (Example)
```rust
trait Foo<'foo>: 'foo {}
trait Bar<'bar>: 'bar {}

trait FooBar<'foo, 'bar>: Foo<'foo> + Bar<'bar> {}

struct Baz<'foo, 'bar> {
    baz: dyn FooBar<'foo, 'bar>,
}
```

---

## 116. E0228 

### 概要 (English Summary)
The lifetime bound for this object type cannot be deduced from context and must

### 日本語解説・原因
`E0228` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The lifetime bound for this object type cannot be deduced from context and must

### コード例 (Example)
```rust
trait Trait { }

struct TwoBounds<'a, 'b, T: Sized + 'a + 'b> {
    x: &'a i32,
    y: &'b i32,
    z: T,
}

type Foo<'a, 'b> = TwoBounds<'a, 'b, dyn Trait>;
```

---

## 117. E0229 

### 概要 (English Summary)
An associated item constraint was written in an unexpected context.

### 日本語解説・原因
`E0229` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An associated item constraint was written in an unexpected context.

### コード例 (Example)
```rust
pub trait Foo {
    type A;
    fn boo(&self) -> <Self as Foo>::A;
}

struct Bar;

impl Foo for isize {
    type A = usize;
    fn boo(&self) -> usize { 42 }
}

fn baz<I>(x: &<I as Foo<A = Bar>>::A) {}
// error: associated item constraint are not allowed here
```

---

## 118. E0230 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
The `#[rustc_on_unimplemented]` attribute used to raise this error for various

### 日本語解説・原因
このエラーコード（`E0230`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 119. E0231 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
詳細な説明はありません。

### 日本語解説・原因
このエラーコード（`E0231`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 120. E0232 

### 概要 (English Summary)
The `#[rustc_on_unimplemented]` attribute lets you specify a custom error

### 日本語解説・原因
`E0232` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `#[rustc_on_unimplemented]` attribute lets you specify a custom error

### コード例 (Example)
```rust
#![feature(rustc_attrs)]
#![allow(internal_features)]

#[rustc_on_unimplemented(on(blah, message = "foo"))] // error!
trait BadAnnotation {}
```

---

## 121. E0243 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This error indicates that not enough type parameters were found in a type or

### 日本語解説・原因
このエラーコード（`E0243`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
struct Foo<T> { x: T }

struct Bar { x: Foo }
```

---

## 122. E0244 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This error indicates that too many type parameters were found in a type or

### 日本語解説・原因
このエラーコード（`E0244`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
struct Foo { x: bool }

struct Bar<S, T> { x: Foo<S, T> }
```

---

## 123. E0251 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Two items of the same name cannot be imported without rebinding one of the

### 日本語解説・原因
このエラーコード（`E0251`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
use foo::baz;
use bar::*; // error, do `use foo::baz as quux` instead on the previous line

fn main() {}

mod foo {
    pub struct baz;
}

mod bar {
    pub mod baz {}
}
```

---

## 124. E0252 

### 概要 (English Summary)
Two items of the same name cannot be imported without rebinding one of the

### 日本語解説・原因
`E0252` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Two items of the same name cannot be imported without rebinding one of the

### コード例 (Example)
```rust
use foo::baz;
use bar::baz; // error, do `use bar::baz as quux` instead

fn main() {}

mod foo {
    pub struct baz;
}

mod bar {
    pub mod baz {}
}
```

---

## 125. E0253 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Attempt was made to import an unimportable type. This can happen when trying

### 日本語解説・原因
このエラーコード（`E0253`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(import_trait_associated_functions)]

mod foo {
    pub trait MyTrait {
        type SomeType;
    }
}

use foo::MyTrait::SomeType;
// error: `SomeType` is not directly importable

fn main() {}
```

---

## 126. E0254 

### 概要 (English Summary)
Attempt was made to import an item whereas an extern crate with this name has

### 日本語解説・原因
`E0254` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Attempt was made to import an item whereas an extern crate with this name has

### コード例 (Example)
```rust
extern crate core;

mod foo {
    pub trait core {
        fn do_something();
    }
}

use foo::core;  // error: an extern crate named `core` has already
                //        been imported in this module

fn main() {}
```

---

## 127. E0255 

### 概要 (English Summary)
You can't import a value whose name is the same as another value defined in the

### 日本語解説・原因
`E0255` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: You can't import a value whose name is the same as another value defined in the

### コード例 (Example)
```rust
use bar::foo; // error: an item named `foo` is already in scope

fn foo() {}

mod bar {
     pub fn foo() {}
}

fn main() {}
```

---

## 128. E0256 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
You can't import a type or module when the name of the item being imported is

### 日本語解説・原因
このエラーコード（`E0256`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
use foo::Bar; // error

type Bar = u32;

mod foo {
    pub mod Bar { }
}

fn main() {}
```

---

## 129. E0259 

### 概要 (English Summary)
The name chosen for an external crate conflicts with another external crate

### 日本語解説・原因
`E0259` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The name chosen for an external crate conflicts with another external crate

### コード例 (Example)
```rust
extern crate core;
extern crate std as core;

fn main() {}
```

---

## 130. E0260 

### 概要 (English Summary)
The name for an item declaration conflicts with an external crate's name.

### 日本語解説・原因
`E0260` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The name for an item declaration conflicts with an external crate's name.

### コード例 (Example)
```rust
extern crate core;

struct core;

fn main() {}
```

---

## 131. E0261 

### 概要 (English Summary)
An undeclared lifetime was used.

### 日本語解説・原因
`E0261` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An undeclared lifetime was used.

### コード例 (Example)
```rust
// error, use of undeclared lifetime name `'a`
fn foo(x: &'a str) { }

struct Foo {
    // error, use of undeclared lifetime name `'a`
    x: &'a str,
}
```

---

## 132. E0262 

### 概要 (English Summary)
An invalid name was used for a lifetime parameter.

### 日本語解説・原因
`E0262` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An invalid name was used for a lifetime parameter.

### コード例 (Example)
```rust
// error, invalid lifetime parameter name `'static`
fn foo<'static>(x: &'static str) { }
```

---

## 133. E0263 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
A lifetime was declared more than once in the same scope.

### 日本語解説・原因
このエラーコード（`E0263`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn foo<'a, 'b, 'a>(x: &'a str, y: &'b str, z: &'a str) { // error!
}
```

---

## 134. E0264 

### 概要 (English Summary)
An unknown external lang item was used.

### 日本語解説・原因
`E0264` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An unknown external lang item was used.

### コード例 (Example)
```rust
#![feature(lang_items)]
#![allow(internal_features)]

extern "C" {
    #[lang = "copy"] // error: unknown external lang item: `copy`
    fn copy();
}
```

---

## 135. E0267 

### 概要 (English Summary)
A loop keyword (`break` or `continue`) was used inside a closure but outside of

### 日本語解説・原因
`E0267` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A loop keyword (`break` or `continue`) was used inside a closure but outside of

### コード例 (Example)
```rust
let w = || { break; }; // error: `break` inside of a closure
```

---

## 136. E0268 

### 概要 (English Summary)
A loop keyword (`break` or `continue`) was used outside of a loop.

### 日本語解説・原因
`E0268` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A loop keyword (`break` or `continue`) was used outside of a loop.

### コード例 (Example)
```rust
fn some_func() {
    break; // error: `break` outside of a loop
}
```

---

## 137. E0271 

### 概要 (English Summary)
A type mismatched an associated type of a trait.

### 日本語解説・原因
`E0271` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type mismatched an associated type of a trait.

### コード例 (Example)
```rust
trait Trait { type AssociatedType; }

fn foo<T>(t: T) where T: Trait<AssociatedType=u32> {
//                    ~~~~~~~~ ~~~~~~~~~~~~~~~~~~
//                        |            |
//         This says `foo` can         |
//           only be used with         |
//              some type that         |
//         implements `Trait`.         |
//                                     |
//                             This says not only must
//                             `T` be an impl of `Trait`
//                             but also that the impl
//                             must assign the type `u32`
//                             to the associated type.
    println!("in foo");
}

impl Trait for i8 { type AssociatedType = &'static str; }
//~~~~~~~~~~~~~~~   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
//      |                             |
// `i8` does have                     |
// implementation                     |
// of `Trait`...                      |
//                     ... but it is an implementation
//                     that assigns `&'static str` to
//                     the associated type.

foo(3_i8);
// Here, we invoke `foo` with an `i8`, which does not satisfy
// the constraint `<i8 as Trait>::AssociatedType=u32`, and
// therefore the type-checker complains with this error code.
```

---

## 138. E0275 

### 概要 (English Summary)
An evaluation of a trait requirement overflowed.

### 日本語解説・原因
`E0275` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An evaluation of a trait requirement overflowed.

### コード例 (Example)
```rust
trait Foo {}

struct Bar<T>(T);

impl<T> Foo for T where Bar<T>: Foo {}
```

---

## 139. E0276 

### 概要 (English Summary)
A trait implementation has stricter requirements than the trait definition.

### 日本語解説・原因
`E0276` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A trait implementation has stricter requirements than the trait definition.

### コード例 (Example)
```rust
trait Foo {
    fn foo<T>(x: T);
}

impl Foo for bool {
    fn foo<T>(x: T) where T: Copy {}
}
```

---

## 140. E0277 

### 概要 (English Summary)
You tried to use a type which doesn't implement some trait in a place which

### 日本語解説・原因
`E0277` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: You tried to use a type which doesn't implement some trait in a place which

### コード例 (Example)
```rust
// here we declare the Foo trait with a bar method
trait Foo {
    fn bar(&self);
}

// we now declare a function which takes an object implementing the Foo trait
fn some_func<T: Foo>(foo: T) {
    foo.bar();
}

fn main() {
    // we now call the method with the i32 type, which doesn't implement
    // the Foo trait
    some_func(5i32); // error: the trait bound `i32 : Foo` is not satisfied
}
```

---

## 141. E0281 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
You tried to supply a type which doesn't implement some trait in a location

### 日本語解説・原因
このエラーコード（`E0281`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn foo<F: Fn(usize)>(x: F) { }

fn main() {
    // type mismatch: ... implements the trait `core::ops::Fn<(String,)>`,
    // but the trait `core::ops::Fn<(usize,)>` is required
    // [E0281]
    foo(|y: String| { });
}
```

---

## 142. E0282 

### 概要 (English Summary)
The compiler could not infer a type and asked for a type annotation.

### 日本語解説・原因
`E0282` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The compiler could not infer a type and asked for a type annotation.

### コード例 (Example)
```rust
let x = Vec::new();
```

---

## 143. E0283 

### 概要 (English Summary)
The compiler could not infer a type and asked for a type annotation.

### 日本語解説・原因
`E0283` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The compiler could not infer a type and asked for a type annotation.

### コード例 (Example)
```rust
let x = "hello".chars().rev().collect();
```

---

## 144. E0284 

### 概要 (English Summary)
This error occurs when the compiler is unable to unambiguously infer the

### 日本語解説・原因
`E0284` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error occurs when the compiler is unable to unambiguously infer the

### コード例 (Example)
```rust
fn main() {
    let n: u32 = 1;
    let mut d: u64 = 2;
    d = d + n.into();
}
```

---

## 145. E0297 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Patterns used to bind names must be irrefutable. That is, they must guarantee

### 日本語解説・原因
このエラーコード（`E0297`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
let xs : Vec<Option<i32>> = vec![Some(1), None];

// This fails because `None` is not covered.
for Some(x) in xs {
    // ...
}
```

---

## 146. E0301 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Mutable borrows are not allowed in pattern guards, because matching cannot have

### 日本語解説・原因
このエラーコード（`E0301`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
match Some(()) {
    None => { },
    option if option.take().is_none() => {
        /* impossible, option is `Some` */
    },
    Some(_) => { } // When the previous match failed, the option became `None`.
}
```

---

## 147. E0302 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Assignments are not allowed in pattern guards, because matching cannot have

### 日本語解説・原因
このエラーコード（`E0302`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
match Some(()) {
    None => { },
    option if { option = None; false } => { },
    Some(_) => { } // When the previous match failed, the option became `None`.
}
```

---

## 148. E0303 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Sub-bindings, e.g. `ref x @ Some(ref y)` are now allowed under

### 日本語解説・原因
このエラーコード（`E0303`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
match Some("hi".to_string()) {
    ref op_string_ref @ Some(s) => {},
    None => {},
}
```

---

## 149. E0307 

### 概要 (English Summary)
The `self` parameter in a method has an invalid "receiver type".

### 日本語解説・原因
`E0307` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `self` parameter in a method has an invalid "receiver type".

### コード例 (Example)
```rust
struct Foo;
struct Bar;

trait Trait {
    fn foo(&self);
}

impl Trait for Foo {
    fn foo(self: &Bar) {}
}
```

---

## 150. E0308 

### 概要 (English Summary)
Expected type did not match the received type.

### 日本語解説・原因
`E0308` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Expected type did not match the received type.

### コード例 (Example)
```rust
fn plus_one(x: i32) -> i32 {
    x + 1
}

plus_one("Not a number");
//       ^^^^^^^^^^^^^^ expected `i32`, found `&str`

if "Not a bool" {
// ^^^^^^^^^^^^ expected `bool`, found `&str`
}

let x: f32 = "Not a float";
//     ---   ^^^^^^^^^^^^^ expected `f32`, found `&str`
//     |
//     expected due to this
```

---

## 151. E0309 

### 概要 (English Summary)
A parameter type is missing an explicit lifetime bound and may not live long

### 日本語解説・原因
`E0309` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A parameter type is missing an explicit lifetime bound and may not live long

### コード例 (Example)
```rust
// This won't compile because the applicable impl of
// `SomeTrait` (below) requires that `T: 'a`, but the struct does
// not have a matching where-clause.
struct Foo<'a, T> {
    foo: <T as SomeTrait<'a>>::Output,
}

trait SomeTrait<'a> {
    type Output;
}

impl<'a, T> SomeTrait<'a> for T
where
    T: 'a,
{
    type Output = u32;
}
```

---

## 152. E0310 

### 概要 (English Summary)
A parameter type is missing a lifetime constraint or has a lifetime that

### 日本語解説・原因
`E0310` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A parameter type is missing a lifetime constraint or has a lifetime that

### コード例 (Example)
```rust
// This won't compile because T is not constrained to the static lifetime
// the reference needs
struct Foo<T> {
    foo: &'static T
}
```

---

## 153. E0311 

### 概要 (English Summary)
This error occurs when there is an unsatisfied outlives bound involving an

### 日本語解説・原因
`E0311` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: This error occurs when there is an unsatisfied outlives bound involving an

### コード例 (Example)
```rust
fn no_restriction<T>(x: &()) -> &() {
    with_restriction::<T>(x)
}

fn with_restriction<'a, T: 'a>(x: &'a ()) -> &'a () {
    x
}
```

---

## 154. E0312 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
Reference's lifetime of borrowed content doesn't match the expected lifetime.

### 日本語解説・原因
このエラーコード（`E0312`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
pub fn opt_str<'a>(maybestr: &'a Option<String>) -> &'static str {
    if maybestr.is_none() {
        "(none)"
    } else {
        let s: &'a str = maybestr.as_ref().unwrap();
        s  // Invalid lifetime!
    }
}
```

---

## 155. E0316 

### 概要 (English Summary)
A `where` clause contains a nested quantification over lifetimes.

### 日本語解説・原因
`E0316` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A `where` clause contains a nested quantification over lifetimes.

### コード例 (Example)
```rust
trait Tr<'a, 'b> {}

fn foo<T>(t: T)
where
    for<'a> &'a T: for<'b> Tr<'a, 'b>, // error: nested quantification
{
}
```

---

## 156. E0317 

### 概要 (English Summary)
An `if` expression is missing an `else` block.

### 日本語解説・原因
`E0317` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An `if` expression is missing an `else` block.

### コード例 (Example)
```rust
let x = 5;
let a = if x == 5 {
    1
};
```

---

## 157. E0320 

### 概要 (English Summary)
Recursion limit reached while creating drop-check rules.

### 日本語解説・原因
`E0320` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Recursion limit reached while creating drop-check rules.

### コード例 (Example)
```rust
enum A<T> {
    B,
    C(T, Box<A<(T, T)>>)
}

fn foo<T>() {
    A::<T>::B; // error: overflow while adding drop-check rules for A<T>
}
```

---

## 158. E0321 

### 概要 (English Summary)
A cross-crate opt-out trait was implemented on something which wasn't a struct

### 日本語解説・原因
`E0321` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A cross-crate opt-out trait was implemented on something which wasn't a struct

### コード例 (Example)
```rust
#![feature(auto_traits)]

struct Foo;

impl !Sync for Foo {}

unsafe impl Send for &'static Foo {}
// error: cross-crate traits with a default impl, like `core::marker::Send`,
//        can only be implemented for a struct/enum type, not
//        `&'static Foo`
```

---

## 159. E0322 

### 概要 (English Summary)
A built-in trait was implemented explicitly. All implementations of the trait

### 日本語解説・原因
`E0322` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A built-in trait was implemented explicitly. All implementations of the trait

### コード例 (Example)
```rust
struct Foo;

impl Sized for Foo {} // error!
```

---

## 160. E0323 

### 概要 (English Summary)
An associated const was implemented when another trait item was expected.

### 日本語解説・原因
`E0323` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An associated const was implemented when another trait item was expected.

### コード例 (Example)
```rust
trait Foo {
    type N;
}

struct Bar;

impl Foo for Bar {
    const N : u32 = 0;
    // error: item `N` is an associated const, which doesn't match its
    //        trait `<Bar as Foo>`
}
```

---

## 161. E0324 

### 概要 (English Summary)
A method was implemented when another trait item was expected.

### 日本語解説・原因
`E0324` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A method was implemented when another trait item was expected.

### コード例 (Example)
```rust
struct Bar;

trait Foo {
    const N : u32;

    fn M();
}

impl Foo for Bar {
    fn N() {}
    // error: item `N` is an associated method, which doesn't match its
    //        trait `<Bar as Foo>`
}
```

---

## 162. E0325 

### 概要 (English Summary)
An associated type was implemented when another trait item was expected.

### 日本語解説・原因
`E0325` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An associated type was implemented when another trait item was expected.

### コード例 (Example)
```rust
struct Bar;

trait Foo {
    const N : u32;
}

impl Foo for Bar {
    type N = u32;
    // error: item `N` is an associated type, which doesn't match its
    //        trait `<Bar as Foo>`
}
```

---

## 163. E0326 

### 概要 (English Summary)
An implementation of a trait doesn't match the type constraint.

### 日本語解説・原因
`E0326` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An implementation of a trait doesn't match the type constraint.

### コード例 (Example)
```rust
trait Foo {
    const BAR: bool;
}

struct Bar;

impl Foo for Bar {
    const BAR: u32 = 5; // error, expected bool, found u32
}
```

---

## 164. E0328 

### 概要 (English Summary)
The Unsize trait should not be implemented directly. All implementations of

### 日本語解説・原因
`E0328` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The Unsize trait should not be implemented directly. All implementations of

### コード例 (Example)
```rust
#![feature(unsize)]

use std::marker::Unsize;

pub struct MyType;

impl<T> Unsize<T> for MyType {}
```

---

## 165. E0329 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An attempt was made to access an associated constant through either a generic

### 日本語解説・原因
このエラーコード（`E0329`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
trait Foo {
    const BAR: f64;
}

struct MyStruct;

impl Foo for MyStruct {
    const BAR: f64 = 0f64;
}

fn get_bar_bad<F: Foo>(t: F) -> f64 {
    F::BAR
}
```

---

## 166. E0364 

### 概要 (English Summary)
Private items cannot be publicly re-exported. This error indicates that you

### 日本語解説・原因
`E0364` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Private items cannot be publicly re-exported. This error indicates that you

### コード例 (Example)
```rust
mod a {
    fn foo() {}

    mod a {
        pub use super::foo; // error!
    }
}
```

---

## 167. E0365 

### 概要 (English Summary)
Private modules cannot be publicly re-exported. This error indicates that you

### 日本語解説・原因
`E0365` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Private modules cannot be publicly re-exported. This error indicates that you

### コード例 (Example)
```rust
mod foo {
    pub const X: u32 = 1;
}

pub use foo as foo2;

fn main() {}
```

---

## 168. E0366 

### 概要 (English Summary)
An attempt was made to implement `Drop` on a concrete specialization of a

### 日本語解説・原因
`E0366` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An attempt was made to implement `Drop` on a concrete specialization of a

### コード例 (Example)
```rust
struct Foo<T> {
    t: T
}

impl Drop for Foo<u32> {
    fn drop(&mut self) {}
}
```

---

## 169. E0367 

### 概要 (English Summary)
An attempt was made to implement `Drop` on a specialization of a generic type.

### 日本語解説・原因
`E0367` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An attempt was made to implement `Drop` on a specialization of a generic type.

### コード例 (Example)
```rust
trait Foo {}

struct MyStruct<T> {
    t: T
}

impl<T: Foo> Drop for MyStruct<T> {
    fn drop(&mut self) {}
}
```

---

## 170. E0368 

### 概要 (English Summary)
A binary assignment operator like `+=` or `^=` was applied to a type that

### 日本語解説・原因
`E0368` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A binary assignment operator like `+=` or `^=` was applied to a type that

### コード例 (Example)
```rust
let mut x = 12f32; // error: binary operation `<<` cannot be applied to
                   //        type `f32`

x <<= 2;
```

---

## 171. E0369 

### 概要 (English Summary)
A binary operation was attempted on a type which doesn't support it.

### 日本語解説・原因
`E0369` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A binary operation was attempted on a type which doesn't support it.

### コード例 (Example)
```rust
let x = 12f32; // error: binary operation `<<` cannot be applied to
               //        type `f32`

x << 2;
```

---

## 172. E0370 

### 概要 (English Summary)
The maximum value of an enum was reached, so it cannot be automatically

### 日本語解説・原因
`E0370` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The maximum value of an enum was reached, so it cannot be automatically

### コード例 (Example)
```rust
#[repr(i64)]
enum Foo {
    X = 0x7fffffffffffffff,
    Y, // error: enum discriminant overflowed on value after
       //        9223372036854775807: i64; set explicitly via
       //        Y = -9223372036854775808 if that is desired outcome
}
```

---

## 173. E0371 

### 概要 (English Summary)
A trait was implemented on another which already automatically implemented it.

### 日本語解説・原因
`E0371` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A trait was implemented on another which already automatically implemented it.

### コード例 (Example)
```rust
trait Foo { fn foo(&self) { } }
trait Bar: Foo { }
trait Baz: Bar { }

impl Bar for Baz { } // error, `Baz` implements `Bar` by definition
impl Foo for Baz { } // error, `Baz` implements `Bar` which implements `Foo`
impl Baz for Baz { } // error, `Baz` (trivially) implements `Baz`
impl Baz for Bar { } // Note: This is OK
```

---

## 174. E0373 

### 概要 (English Summary)
A captured variable in a closure may not live long enough.

### 日本語解説・原因
`E0373` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A captured variable in a closure may not live long enough.

### コード例 (Example)
```rust
fn foo() -> Box<dyn Fn(u32) -> u32> {
    let x = 0u32;
    Box::new(|y| x + y)
}
```

---

## 175. E0374 

### 概要 (English Summary)
`CoerceUnsized` or `DispatchFromDyn` was implemented on a struct which does not

### 日本語解説・原因
`E0374` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `CoerceUnsized` or `DispatchFromDyn` was implemented on a struct which does not

### コード例 (Example)
```rust
#![feature(coerce_unsized)]
use std::ops::CoerceUnsized;

struct Foo<T: ?Sized> {
    a: i32,
}

// error: Struct `Foo` has no unsized fields that need to be coerced.
impl<T, U> CoerceUnsized<Foo<U>> for Foo<T>
    where T: CoerceUnsized<U> {}
```

---

## 176. E0375 

### 概要 (English Summary)
`CoerceUnsized` or `DispatchFromDyn` was implemented on a struct which contains

### 日本語解説・原因
`E0375` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `CoerceUnsized` or `DispatchFromDyn` was implemented on a struct which contains

### コード例 (Example)
```rust
#![feature(coerce_unsized)]
use std::ops::CoerceUnsized;

struct Foo<T: ?Sized, U: ?Sized> {
    a: i32,
    b: T,
    c: U,
}

// error: Struct `Foo` has more than one unsized field.
impl<T, U> CoerceUnsized<Foo<U, T>> for Foo<T, U> {}
```

---

## 177. E0376 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
`CoerceUnsized` or `DispatchFromDyn` was implemented between two types that

### 日本語解説・原因
このエラーコード（`E0376`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(coerce_unsized)]
use std::ops::CoerceUnsized;

struct Foo<T: ?Sized> {
    a: T,
}

// error: The type `U` is not a struct
impl<T, U> CoerceUnsized<U> for Foo<T> {}
```

---

## 178. E0377 

### 概要 (English Summary)
`CoerceUnsized` or `DispatchFromDyn` may only be implemented between structs

### 日本語解説・原因
`E0377` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: `CoerceUnsized` or `DispatchFromDyn` may only be implemented between structs

### コード例 (Example)
```rust
#![feature(coerce_unsized)]
use std::ops::CoerceUnsized;

pub struct Foo<T: ?Sized> {
    field_with_unsized_type: T,
}

pub struct Bar<T: ?Sized> {
    field_with_unsized_type: T,
}

// error: the trait `CoerceUnsized` may only be implemented for a coercion
//        between structures with the same definition
impl<T, U> CoerceUnsized<Bar<U>> for Foo<T> where T: CoerceUnsized<U> {}
```

---

## 179. E0378 

### 概要 (English Summary)
The `DispatchFromDyn` trait was implemented on something which is not a pointer

### 日本語解説・原因
`E0378` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The `DispatchFromDyn` trait was implemented on something which is not a pointer

### コード例 (Example)
```rust
#![feature(dispatch_from_dyn)]
use std::ops::DispatchFromDyn;

struct WrapperExtraField<T> {
    ptr: T,
    extra_stuff: i32,
}

impl<T, U> DispatchFromDyn<WrapperExtraField<U>> for WrapperExtraField<T>
where
    T: DispatchFromDyn<U>,
{}
```

---

## 180. E0379 

### 概要 (English Summary)
A trait method was declared const.

### 日本語解説・原因
`E0379` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A trait method was declared const.

### コード例 (Example)
```rust
trait Foo {
    const fn bar() -> u32; // error!
}

impl Foo for () {
    const fn bar() -> u32 { 0 } // error!
}
```

---

## 181. E0380 

### 概要 (English Summary)
An auto trait was declared with a method or an associated item.

### 日本語解説・原因
`E0380` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An auto trait was declared with a method or an associated item.

### コード例 (Example)
```rust
unsafe auto trait Trait {
    type Output; // error!
}
```

---

## 182. E0381 

### 概要 (English Summary)
It is not allowed to use or capture an uninitialized variable.

### 日本語解説・原因
`E0381` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: It is not allowed to use or capture an uninitialized variable.

### コード例 (Example)
```rust
fn main() {
    let x: i32;
    let y = x; // error, use of possibly-uninitialized variable
}
```

---

## 183. E0382 

### 概要 (English Summary)
A variable was used after its contents have been moved elsewhere.

### 日本語解説・原因
`E0382` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A variable was used after its contents have been moved elsewhere.

### コード例 (Example)
```rust
struct MyStruct { s: u32 }

fn main() {
    let mut x = MyStruct{ s: 5u32 };
    let y = x;
    x.s = 6;
    println!("{}", x.s);
}
```

---

## 184. E0383 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This error occurs when an attempt is made to partially reinitialize a

### 日本語解説・原因
このエラーコード（`E0383`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
struct Foo {
    a: u32,
}
impl Drop for Foo {
    fn drop(&mut self) { /* ... */ }
}

let mut x = Foo { a: 1 };
drop(x); // `x` is now uninitialized
x.a = 2; // error, partial reinitialization of uninitialized structure `t`
```

---

## 185. E0384 

### 概要 (English Summary)
An immutable variable was reassigned.

### 日本語解説・原因
`E0384` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: An immutable variable was reassigned.

### コード例 (Example)
```rust
fn main() {
    let x = 3;
    x = 5; // error, reassignment of immutable variable
}
```

---

## 186. E0386 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This error occurs when an attempt is made to mutate the target of a mutable

### 日本語解説・原因
このエラーコード（`E0386`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
let mut x: i64 = 1;
let y: Box<_> = Box::new(&mut x);
**y = 2; // error, cannot assign to data in an immutable container
```

---

## 187. E0387 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
This error occurs when an attempt is made to mutate or mutably reference data

### 日本語解説・原因
このエラーコード（`E0387`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
// Accepts a function or a closure that captures its environment immutably.
// Closures passed to foo will not be able to mutate their closed-over state.
fn foo<F: Fn()>(f: F) { }

// Attempts to mutate closed-over data. Error message reads:
// `cannot assign to data in a captured outer variable...`
fn mutable() {
    let mut x = 0u32;
    foo(|| x = 2);
}

// Attempts to take a mutable reference to closed-over data. Error message
// reads: `cannot borrow data mutably in a captured outer variable...`
fn mut_addr() {
    let mut x = 0u32;
    foo(|| { let y = &mut x; });
}
```

---

## 188. E0388 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
詳細な説明はありません。

### 日本語解説・原因
このエラーコード（`E0388`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

---

## 189. E0389 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
An attempt was made to mutate data using a non-mutable reference. This

### 日本語解説・原因
このエラーコード（`E0389`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
struct FancyNum {
    num: u8,
}

fn main() {
    let mut fancy = FancyNum{ num: 5 };
    let fancy_ref = &(&mut fancy);
    fancy_ref.num = 6; // error: cannot assign to data in a `&` reference
    println!("{}", fancy_ref.num);
}
```

---

## 190. E0390 

### 概要 (English Summary)
A method or constant was implemented on a primitive type.

### 日本語解説・原因
`E0390` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A method or constant was implemented on a primitive type.

### コード例 (Example)
```rust
struct Foo {
    x: i32
}

impl *mut Foo {}
// error: cannot define inherent `impl` for primitive types
```

---

## 191. E0391 

### 概要 (English Summary)
A type dependency cycle has been encountered.

### 日本語解説・原因
`E0391` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type dependency cycle has been encountered.

### コード例 (Example)
```rust
trait FirstTrait : SecondTrait {

}

trait SecondTrait : FirstTrait {

}
```

---

## 192. E0392 

### 概要 (English Summary)
A type or lifetime parameter has been declared but is not actually used.

### 日本語解説・原因
`E0392` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type or lifetime parameter has been declared but is not actually used.

### コード例 (Example)
```rust
enum Foo<T> {
    Bar,
}
```

---

## 193. E0393 

### 概要 (English Summary)
A type parameter which references `Self` in its default value was not specified.

### 日本語解説・原因
`E0393` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type parameter which references `Self` in its default value was not specified.

### コード例 (Example)
```rust
trait A<T = Self> {}

fn together_we_will_rule_the_galaxy(son: &dyn A) {}
// error: the type parameter `T` must be explicitly specified
```

---

## 194. E0398 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
In Rust 1.3, the default object lifetime bounds are expected to change, as

### 日本語解説・原因
このエラーコード（`E0398`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
fn foo(arg: &Box<SomeTrait>) { /* ... */ }
```

---

## 195. E0399 （※ 現在のコンパイラでは廃止/非推奨）

### 概要 (English Summary)
You implemented a trait, overriding one or more of its associated types but did

### 日本語解説・原因
このエラーコード（`E0399`）は古いバージョンの rustc で使用されていましたが、現在のコンパイラでは別のエラーコードに統合されたか、非推奨となっています。

### コード例 (Example)
```rust
#![feature(associated_type_defaults)]

pub trait Foo {
    type Assoc = u8;
    fn bar(&self) {}
}

impl Foo for i32 {
    // error - the following trait items need to be reimplemented as
    //         `Assoc` was overridden: `bar`
    type Assoc = i32;
}
```

---

## 196. E0401 

### 概要 (English Summary)
Inner items do not inherit the generic parameters from the items

### 日本語解説・原因
`E0401` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Inner items do not inherit the generic parameters from the items

### コード例 (Example)
```rust
fn foo<T>(x: T) {
    fn bar(y: T) { // T is defined in the "outer" function
        // ..
    }
    bar(x);
}
```

---

## 197. E0403 

### 概要 (English Summary)
Some type parameters have the same name.

### 日本語解説・原因
`E0403` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: Some type parameters have the same name.

### コード例 (Example)
```rust
fn f<T, T>(s: T, u: T) {} // error: the name `T` is already used for a generic
                          //        parameter in this item's generic parameters
```

---

## 198. E0404 

### 概要 (English Summary)
A type that is not a trait was used in a trait position, such as a bound

### 日本語解説・原因
`E0404` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A type that is not a trait was used in a trait position, such as a bound

### コード例 (Example)
```rust
struct Foo;
struct Bar;

impl Foo for Bar {} // error: `Foo` is not a trait
fn baz<T: Foo>(t: T) {} // error: `Foo` is not a trait
```

---

## 199. E0405 

### 概要 (English Summary)
The code refers to a trait that is not in scope.

### 日本語解説・原因
`E0405` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: The code refers to a trait that is not in scope.

### コード例 (Example)
```rust
struct Foo;

impl SomeTrait for Foo {} // error: trait `SomeTrait` is not in scope
```

---

## 200. E0407 

### 概要 (English Summary)
A definition of a method not in the implemented trait was given in a trait

### 日本語解説・原因
`E0407` は、Rustコンパイラが構文・型・借用・トレイト等の制約違反を検出した際に発生します。

- **英語メッセージ抜粋**: A definition of a method not in the implemented trait was given in a trait

### コード例 (Example)
```rust
trait Foo {
    fn a();
}

struct Bar;

impl Foo for Bar {
    fn a() {}
    fn b() {} // error: method `b` is not a member of trait `Foo`
}
```

---

